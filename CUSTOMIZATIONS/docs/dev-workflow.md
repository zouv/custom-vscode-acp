# 本地开发与验收流程

> **这是什么**：怎么把扩展跑起来、怎么改完立刻看到效果、手动验收时该看哪几个地方。
> 属于**按需查阅**文档——`AGENTS.md` 只留一行指针，避免给每个会话都加这个上下文。
>
> **配套**：代码位置查 [`../architecture.md`](../architecture.md)；历史坑点查 [`pitfalls.md`](./pitfalls.md)。

---

## 启动 Extension Development Host

### 方式一：`npm run dev:host`（推荐）

```bash
npm run dev:host                     # 先编译，再启动宿主窗口
npm run dev:host -- --no-build       # 跳过编译，直接启动
npm run dev:host -- <别的目录>        # 用别的目录当工作区（默认本仓库）
```

默认**先编译再启动**是有意的：忘了编译就会测到旧产物，那是"改了半天没生效"最常见的来源。

实现见 `CUSTOMIZATIONS/scripts/dev-host.mjs`。用 Node 而不是把命令直接写进 `package.json` 的 `scripts`，
是因为 npm 在 Windows 默认走 cmd.exe、macOS/Linux 走 sh，`%CD%` 与 `$PWD` 互不通用，
而 `--extensionDevelopmentPath` 需要**绝对路径**（同 `check-registry.mjs` 从 bash 移植到 Node 的理由，见 pitfalls #8）。

### 方式二：直接敲 code 命令

```powershell
# PowerShell / CMD（code 已在 PATH 上）
code --new-window --extensionDevelopmentPath="D:\Git\zgithub\custom-vscode-acp" "D:\Git\zgithub\custom-vscode-acp"

# Git Bash：编译 + 启动一步到位
npm run compile && code --new-window --extensionDevelopmentPath="D:/Git/zgithub/custom-vscode-acp" "D:/Git/zgithub/custom-vscode-acp"
```

`code` 不在 PATH 时用全路径：`& "D:\Program Files\Microsoft VS Code\bin\code.cmd" --new-window ...`

> `code` 不在 PATH 的永久解法：VS Code 命令面板 → `Shell Command: Install 'code' command in PATH`。

### 方式三：在仓库窗口按 F5

`.vscode/launch.json` 的 "Run Extension"，`preLaunchTask` 是 `npm: watch`（会持续重建）。

### 三种方式的区别

| | `npm run dev:host` | 直接敲 `code` | 仓库窗口按 F5 |
|---|---|---|---|
| 加载扩展 | ✅ | ✅ | ✅ |
| 自动编译 | ✅（可用 `--no-build` 关掉） | ❌ | ✅（watch 任务） |
| **附加调试器**（断点、`launch.json`） | ❌ | ❌ | ✅ |
| 需要先打开仓库窗口 | 不需要 | 不需要 | 必须 |
| 适合 | 手工点击验收 | 脚本里调用 | 断点排查逻辑 |

---

## 改了代码之后怎么重载

在**宿主窗口**（标题带 `[扩展开发宿主]`）按 **`Ctrl+R`** 重载扩展宿主，即可加载新的 `dist/extension.js`。
**不需要关掉重开。**

完整循环：

```
改代码  →  npm run compile  →  宿主窗口 Ctrl+R  →  重新操作验证
```

> **不能**只按保存就生效——宿主加载的是 `dist/extension.js`（webpack 产物），不是 `src/`。
> 这也是 `npm run dev:host` 默认先编译的原因。

---

## 自动化能覆盖到哪、哪些必须人工

**这张表存在的理由**：这个面板的 bug 有两大类，**一类能自动测，一类不能**。
知道分界线在哪，才不会把时间花在重跑 `npm test` 去等一个它看不见的问题。

| 层面 | 自动化？ | 靠什么 / 为什么不能 |
|---|---|---|
| 类型、未使用符号、构建 | ✅ | `npm run compile` / `tsc` / `eslint` |
| 客户端脚本能不能解析（模板字符串里的反引号、转义） | ✅ | `check-registry.mjs` §5 → `check-webview-client.mjs` |
| 机制一致性（标记↔账本、命名空间、换行符、控制字符） | ✅ | `check-registry.mjs` 六节 |
| 嵌套推断的护栏行为 | ✅ | `src/test/nesting.test.ts`（11 条） |
| **宿主发给 webview 的协议流**（记录有没有建出来、有没有永远 streaming、快照与增量是否一致） | ✅ | `src/test/chat-panel.test.ts`（`CUSTOM-20260925-054`），由**真实抓包的 replay 夹具**驱动 |
| **布局与观感**（空白条、条目被压扁、diff 被折叠、按钮化后外观错位、焦点环、滚动跟随） | ❌ **必须人工** | 需要真 webview 渲染；jsdom 不是 Chromium，用它测布局会给出**假信心**（这面板历来的 bug 恰恰都是布局 bug） |
| **键盘与读屏**（Tab 顺序、`aria-busy` 的播报时机、焦点落点） | ❌ **必须人工** | 需要真交互与辅助技术 |
| **需要真实 agent 的行为**（replay 内容、权限弹窗、工具卡内容） | ⚠️ 夹具化后✅ | 协议形状变了要**重抓**夹具（见下）；抓包本身要花额度 |

**重抓 replay 夹具**（只在协议形状变化时才需要）：

```bash
node CUSTOMIZATIONS/scripts/capture-acp-replay.mjs --out src/test/fixtures/claude-code-session-load.json
```

**它真的会拉起 agent、花额度、并在该 agent 的会话历史里留下一个新会话。**
它抓 live 与 replay 两段流写进同一个文件（测试靠这一点做"实时 vs 重开"对比），
在一次性临时目录里跑（绝不在仓库内跑，因为 agent 会读写文件）。

**报告"重开了但内容不对"时的第一步**：不要先看截图，先看 `npm test` 里
`chat-panel: real session/load replay` 那条**诊断**输出——它打印 live 与 replay 的**通知类型直方图**。
`replay:` 里有 `agent_message_chunk` / `agent_thought_chunk` ⇒ **是我们丢了**；
没有 ⇒ agent 没回放，那时该改的是渲染方式，而不是去修丢内容。
（这条判据是 `CUSTOM-20260925-055` 用一整天换来的，见 `docs/pitfalls.md` #22。）

---

## 手动验收时该看哪几个地方

按排查效率排序：

| 位置 | 看什么 |
|---|---|
| **`Developer: Open Webview Developer Tools`** | **webview 客户端 JS 的唯一报错出口**。`src/ui/chat/html/client/*.ts` 是内联脚本，tsc/lint 看不到运行时问题。命令面板里搜 `webview` 即可找到 |
| 输出通道 **`ACP Client (Custom)`** | 扩展侧日志。聊天面板相关搜前缀 `chat-router:`（面板路由决策）与 `chat-panel:`（面板收到的消息与错误）。**性能相关搜 `chat-panel: outbox`**——每轮结束时打一行 `sent=N merged=M queued=K`，`merged` 高说明流式合帧在工作 |
| 输出通道 **`ACP Traffic (Custom)`** | ACP 协议**原始报文**（受 `acpc.logTraffic` 控制，默认开）。很多时候是 agent 没发某条通知，而不是面板没渲染 |
| 命令面板搜 `ACP (Custom)` | 应该列出 22 个命令。少于 22 个说明扩展没完整激活 |

**判断当前是哪块面板**（Chat 面板重写后有两套实现）：

- 顶部**有标签行** + `+` 按钮 → 新面板（`src/ui/chat/`，目前只有 Claude Code 走这条）
- **无标签行**、单会话 → 旧面板（`ChatWebviewProvider`，其余 agent 走这条）
- DevTools Console 里敲 `window.__acpc`：新面板存在该全局对象，旧面板不存在

 **新面板在哪一面**（CUSTOM-20260924-019 起有两面，可同时在）：

- 侧边栏 `acpc-chat` 视图 / 编辑区标签页 `ACP Chat`（`acpc.openChatInEditor` 或 `Ctrl+Alt+A` 打开）
- 两者共享同一份会话记录：同一会话在两边会同步收到相同的流式更新
- 编辑区面板**只对 Claude Code 开放**；聚焦其它 agent 时点命令只会弹一条提示，且已开着的面板会自动关闭

---

## 手动验收清单（Chat 面板重写）

> 这份清单原先只存在于开发计划里，而计划文件不在仓库中——记在这里以免丢失。
> 对应改动：`CUSTOM-20260923-011` ~ `CUSTOM-20260923-013`。

**P0 · 面板可用性**（每条对应一个已修的缺陷）

| # | 怎么试 | ✅ 通过 | ❌ 失败长什么样 |
|---|---|---|---|
| 1 | 发一句带代码块的请求 | 代码块/加粗/列表**渲染出来** | 看到 `###`、`**`、``` ``` ``` 原始字符 → markdown 链断了 |
| 2 | 发一条长回复 | **只有 1 个**助手气泡在增长 | 出现 N 个气泡各装一段 → 流式分片 |
| 3 | 让它"先思考再回答" | 思考块自动折叠，标题 `Thought for Ns` | 一直展开且写 `Thinking…` |
| 4 | 发消息后立刻看按钮 | 变成 `■ Stop` | 仍是 `Send` → composer 没收到 running |
| 5 | 跑一个改文件的工具调用 → 切标签再切回 | 工具卡片内容**还在** | 只剩空白框 |
| 6 | 点 diff 头部 | 展开行级 diff（行号 + `+`/`-` 配色） | 点了没反应 |

**P1 · 功能**

7. 图片/资源类内容块渲染成真实元素（不是 `[image content]` 占位）
8. 多次 plan 更新后只有**一张** plan 卡片且内容最新
9. 执行类工具卡片上的 `▶ terminal` chip 能展开该终端输出
10. 工具卡片里的文件 chip 能跳到对应文件的行

**P2 · 子 agent 分组**（Claude Code 专用，全部是推断）

11. 让它用 Task 起子 agent → 子卡片**缩进**在父卡片下，父卡片显示 `N steps`
12. 子卡片带 `?` 角标 → 走的是**时序猜测**；**没有**角标 → 命中了显式链接（更好的结果）
13. 会话头部 `Sub-agents` 开关能切回扁平；折叠父卡片能连带折叠子卡片

**安全 · 敌意 markdown**（必须全部无害）

```
# H <img src=x onerror=alert(1)>
<script>alert(2)</script>
[x](javascript:alert(3))
![i](javascript:alert(4))
<iframe src="//evil"></iframe>
```

通过标准：**不弹窗**；`<script>` 显示为转义后的字面文本；`javascript:` 链接显示成带删除线的灰字。

**回归**

14. 连一个非 Claude Code 的 agent（如 Gemini CLI）→ 仍是旧面板且行为不变
15. 流式输出时向上滚动 → 焦点**不被拽回底部**，出现 `↓ Jump to latest`

**编辑区面板**（`CUSTOM-20260924-019`）

| # | 怎么试 | ✅ 通过 | ❌ 失败长什么样 |
|---|---|---|---|
| 16 | 侧边栏面板开着时执行 `Open Chat in Editor Area` | 编辑区出现 `ACP Chat` 标签页，内容与侧边栏一致 | 空白标签页 |
| 17 | 在任一面点会话标签切换会话 | 两个面同步切换、都渲染出 markdown | 只有一边切换 / 一边显示原始 markdown |
| 18 | 关掉编辑区标签页再重新打开 | 完整记录回来 | 空白；或 `ACP Traffic` 里出现 `session/load`（不该有） |
| 19 | 面板开着时聚焦一个非 Claude Code 会话 | 编辑区面板自动关闭，侧边栏回到旧面板 | 编辑区残留上一个 agent 的内容 |
| 20 | 聚焦非 Claude Code 会话时执行该命令 | 只弹一条提示 | 打开了一个空面板 |
| 21 | 分别在两个 webview 的 DevTools 里敲 `window.__acpc` | 两边都存在 | 某一边是空白文档（说明监听器注册晚于 attach） |

**面板内权限卡**（`CUSTOM-20260924-020`）

| # | 怎么试 | ✅ 通过 | ❌ 失败长什么样 |
|---|---|---|---|
| 22 | 让 Claude Code 改一个文件（会触发权限请求） | 卡片出现在**对应工具卡之后**，两个面都有；在一个面点允许 → 另一个面的卡片同步变成结果行 | 完全没有卡片 → 看 `chat-panel:` 日志里有没有 `prompt … shown in panel` |
| 23 | 待决时关掉编辑区面板、侧边栏在别的 agent | 弹出 QuickPick（一次只一个、标题带会话名），回答后卡片显示最终结果 | agent 一直不继续（卡片被降级为 deferred 但没人回答） |
| 24 | 两个会话同时请求权限 | 弹框**一次只出一个**，各自标明是哪个会话 | 两个弹框叠在一起、分不清归属 |
| 25 | 待决时按 Stop / Escape | 卡片变 `Cancelled`；`ACP Traffic` 里 `session/cancel` 后跟 `stopReason: cancelled` | agent 永久挂起（说明待决请求没被回答成 cancelled） |
| 26 | 工具标题含 `<img src=x onerror=alert(1)>` | 显示为字面文本、不弹框 | 弹窗 = innerHTML 注入 |
| 27 | 非 Claude Code agent 触发权限 | 仍是原来的 QuickPick | 面板里冒出卡片（协议本不该进 legacy 面板） |

**会话大纲**（`CUSTOM-20260924-021`）

| # | 怎么试 | ✅ 通过 | ❌ 失败长什么样 |
|---|---|---|---|
| 28 | 会话里有若干条用户消息后看标题栏 | 出现 ☰；点开列出每条用户消息（时间 + 首行摘要） | 按钮不出现 / 列表为空 |
| 29 | 点其中一条 | 平滑跳到那条消息，该行高亮；再滚动时高亮跟随 | 跳到不相干的位置（顺序来源错了）/ 高亮不动 |
| 30 | 流式输出中跳到中间 | **不被后续分片拽回底部**；出现 `↓ Jump to latest` | 一有分片就被拉回底部 |
| 31 | 打开抽屉后按 Esc（输入框未聚焦） | 只关抽屉 | 正在跑的轮次被取消 |
| 32 | 无用户消息（刚建的空会话） | ☰ 隐藏 | 空列表抽屉 |

**长会话性能**（`CUSTOM-20260924-022`）

| # | 怎么试 | ✅ 通过 | ❌ 失败长什么样 |
|---|---|---|---|
| 33 | 发一条会产生很长回复的请求（>500 片） | **只有 1 个**气泡在增长、不丢字；输出通道里有 `chat-panel: outbox sent=… merged=…`，`merged` 占比高 | `merged` 一直是 0（合帧没生效）/ 出现多个气泡（INV-A 破了） |
| 34 | 上面那条进行中看 DevTools Performance | 没有 >50ms 的长任务 | 每片一次强制重排（说明 rAF 合帧没接上） |
| 35 | 滚到会话中部 → 切到别的会话 → 切回来 | 回到原来的位置 | 总是跳到底部（说明 hydrate 里残留 `toBottom()` 或 `restore` 没接上） |
| 36 | 停在中部不动，等流式继续 | 位置**不被拽走**；用户自己滚动后就不再有程序写入 | 一有分片就被拉回底部 |
| 37 | 恢复一个旧会话（位置在中部） | markdown 渲染完后位置仍然正确 | 位置被 markdown 撑走（`reassert` 没生效） |
| 38 | 关掉再打开编辑区面板 | 该会话的滚动位置还在 | 位置丢失（`persistUi` 没接上） |
| 39 | 发消息后立刻看按钮 | `■ Stop` **立即**出现 | 延迟或一直是 `Send`（INV-C 破了：`sessionsChanged` 被合帧） |

**左侧引导条**（`CUSTOM-20260924-023`）

| # | 怎么试 | ✅ 通过 | ❌ 失败长什么样 |
|---|---|---|---|
| 40 | 发一句会触发工具调用的请求 | 左侧出现竖线 + 点；用户消息处是**大实心点**，推理/工具是**小空心点** | 点是空的 / 全是大点 / 位置与内容错位 |
| 41 | 工具正在跑 / 跑失败时看它的点 | 进行中是**蓝色**、失败是**红色** | 工具跑完了点还是蓝的（`updateTool` 没存回 view model） |
| 42 | 点任一个点 | 跳到对应步骤；滚动时高亮跟随当前所在步骤 | 点了没反应 / 高亮不动 |
| 43 | 长回复流式输出时看 Performance | 无 >50ms 长任务（签名没变时不读布局） | 每帧全量测 offsetTop |
| 44 | 同时用 ☰ 抽屉 | 两者并存、互不干扰；抽屉打开时引导条仍随内容滚动 | 引导条被挤位 / 挡住左侧文字选区 |
| 45 | 有会话时点标签行的 `+` | **多出一个新标签页**并聚焦；`ACP Traffic` 里出现一次 `session/new` | 什么都没发生（回归：又用回了 `connectToAgent`——它只聚焦已有会话） |
| 46 | 关掉所有标签后点 `+` | 仍能建出新会话 | 点击被静默吞掉（`focusedAgent` 为空时没兜底） |
| 47 | 未连接任何 agent 时点 `+` | 对话区出现一条提示 | 毫无动静 |

**空白条**（`CUSTOM-20260924-026`）

| # | 怎么试 | ✅ 通过 | ❌ 失败长什么样 |
|---|---|---|---|
| 48 | 跑一段长对话（多次工具调用 + 多次推理） | 记录里**没有**空白横条；每个块都有内容 | 成串细白条 = 空文本块又被写成了 `content` 记录；带圆角边框的空盒子 = 空白文本新建了空气泡 |
| 49 | 助手回复中间的**段落换行** | 仍然正常（空行还在） | 段落挤在一起（守卫误伤了"往已有气泡里追加"这条路径） |
| 50 | 内容块渲染（图片 / 资源 chip） | 不受影响 | 真实内容被一起过滤掉 |

**工具卡空壳自愈**（`CUSTOM-20260924-027`）

| # | 怎么试 | ✅ 通过 | ❌ 失败长什么样 |
|---|---|---|---|
| 51 | 让它跑几个工具（读文件 / 搜索 / 执行） | 每张卡都有 kind 标签 + 题目，能展开 | 出现**只有边框、里面空的**卡片（空壳） |
| 52 | 再出现空卡时看输出通道 `ACP Client (Custom)` | 出现 `chat-panel: resending view for tool …`（已自愈）或 `… the invocation is not in the store`（索引里真丢了） | 两行都没有 → 说明是第三条件，把日志发我 |
| 53 | 工具跑完后卡片标题/状态 | 从 "Tool •" 变成真实 kind（Read/Run…）与 ✓ | 一直显示 "Tool"（`update` 没补 kind） |

**记录类型图标**（`CUSTOM-20260924-028`）

| # | 怎么试 | ✅ 通过 | ❌ 失败长什么样 |
|---|---|---|---|
| 54 | 看各类记录左侧 | 用户/助手/推理/计划/通知各有图标，颜色跟随主题 | 某类没有；或图标是黑的看不清 |
| 55 | 助手长回复**流式输出中** | 图标一直在，不闪不消失 | 图标被每片文本重写抹掉（挂载点选错了） |
| 56 | 推理块收束（出现 `Thought for Ns`）后 | 图标仍在 | 图标没了（收束时那次重写没保留它） |
| 57 | 助手气泡宽度 | 仍然占满（row 布局没把气泡压成内容宽度） | 气泡缩成一条 |

**编辑区面板不再依赖侧边栏**（`CUSTOM-20260925-031`）

| # | 怎么试 | ✅ 通过 | ❌ 失败长什么样 |
|---|---|---|---|
| 58 | **重载窗口后不点任何会话**，直接点 Agents 视图标题栏的按钮 | 面板打开且**自带内容**（自动聚焦 Claude Code 最近的会话） | 弹「Claude Code only」提示（又按"聚焦 agent 必须是 modern"判了） |
| 59 | 上面那步之后看输出通道 | `chat-panel: no focused session; focusing Claude Code's most recent (…)` | 没有这行 |
| 60 | 面板开着时聚焦一个**非 Claude Code** 会话 | 编辑区面板**自动关闭** | 面板留着显示别的 agent 的陈旧内容（不变量破了） |
| 61 | 完全没连接任何 agent 时点按钮 | 面板打开、显示空态 | 弹提示 |
| 62 | 重载窗口后点按钮 → 面板里点会话标签切换 | 正常切换（说明自动聚焦拿到的上下文是新的，不是空上下文） | 面板空白 / 切不动 |

**面板内连接 + 历史会话**（`CUSTOM-20260925-032/033`）

| # | 怎么试 | ✅ 通过 | ❌ 失败长什么样 |
|---|---|---|---|
| 63 | 未连接任何 agent 时看面板空态 | 出现「Connect Claude Code」按钮 | 没有按钮 / 文案不对 |
| 64 | 点它 | Claude Code 被拉起并出现一个会话标签（`ACP Traffic` 有 `initialize` + `session/new`） | 毫无动静 |
| 65 | 已有会话时再点 | **只是聚焦**，不新建第二个 | 又开了一个会话（说明用错成 `newConversation` 了） |
| 66 | 点标题栏 ↺ | 拉出历史会话列表，头部写明条数与来源（agent / 本地缓存） | 空列表 / 没反应 |
| 67 | **未连接**时点 ↺ | 只列本地缓存，`ACP Traffic` 里**没有** `initialize`（没为渲染菜单而 spawn） | 出现 `initialize` |
| 68 | 点列表里的一条 | `session/load` 重放，面板出现完整记录 | 打不开 / 报错 |
| 69 | 浮层打开时按 Esc | 只关浮层，不取消轮次 | 正在跑的轮次被取消 |

**键盘可达性**（`CUSTOM-20260925-047`）——**本轮最需要人工确认的一项**

| # | 怎么试 | ✅ 通过 | ❌ 失败长什么样 |
|---|---|---|---|
| 71 | 全程不用鼠标：`Ctrl+Shift+A` 聚焦面板 → Tab 到标签行 → `←`/`→` 切换会话 | 会话真的切换，焦点停在新的激活标签上（焦点环可见） | 焦点掉回 body（`tabFocusExpected` 没生效），或外观错位 |
| 72 | 标签上按 `Delete` | 该会话被关闭 | 没反应 |
| 73 | Tab 到工具卡头或 diff 头，按 `Enter`/`Space` | 展开/折叠，且读屏能听出展开态（`aria-expanded`） | 没反应（该节点还不是 `<button>`） |
| 74 | Tab 到文件 chip，按 `Enter` | 打开该文件 | 没反应 |
| 75 | 只按 Tab 走一遍整条 transcript（几百条工具卡） | 每个**可点**控件都能到达 | 大量不可点元素占着 tab 位（打不开的 `resource_link` 应是 `<span>`） |
| 76 | 引导条上的点用 `↑`/`↓` | 视口逐个跳转、当前点高亮 | 没反应；或整个引导条有几百个 tab 停靠位（roving tabindex 没生效） |
| 77 | **鼠标**点击各处 | **外观与改动前逐处一致**（按钮化最容易出问题的是内边距与对齐） | 出现外框 / 灰底 / 字体变化（UA 按钮样式没被抹平） |

**读屏播报**（`CUSTOM-20260925-048`）

| # | 怎么试 | ✅ 通过 | ❌ 失败长什么样 |
|---|---|---|---|
| 78 | 开 NVDA/VoiceOver 跑一轮 | 助手回答**结束时**听到一次完整内容 | 流式期间被反复打断 → 看 `#messages` 的 `aria-busy` 有没有跟着流式状态翻 |

**附件与草稿**（`CUSTOM-20260925-049` / `050`）

| # | 怎么试 | ✅ 通过 | ❌ 失败长什么样 |
|---|---|---|---|
| 79 | 从 VS Code 资源管理器拖文件进面板 | 出现附件 chip；拖拽期间 `.message-area` 有虚线边框 | 毫无反应；或出现「Could not read the dropped file location」（说明 `text/uri-list` 与 `files[].path` 都没取到，属已知边界） |
| 80 | 从**系统**文件管理器（VS Code 之外）拖 | 取到路径则加 chip；取不到则**明确提示** | 静默无反应——这正是 049 特意要消灭的失败形态 |
| 81 | 输入框里粘贴一张截图 | 明确提示"暂不支持粘贴图片" | 毫无反应 |
| 82 | 给会话 A 打半句 → 切到 B → 切回 A | A 的草稿还在，B 的输入框是干净的 | 草稿丢失；或 A 的草稿出现在 B 里（`stashDraft` 的调用顺序错了） |
| 83 | 侧边栏与编辑区同时打开 | 编辑区底色 = 同组编辑器底色；侧边栏保持原样 | 编辑区仍是侧边栏底色 |
| 84 | 系统开"减少动效" | 无脉动点 / 无旋转圈 | 仍在动 |

**代码块语言标签**（`CUSTOM-20260925-051`）

| # | 怎么试 | ✅ 通过 | ❌ 失败长什么样 |
|---|---|---|---|
| 85 | 让回复里带语言围栏的代码块 | 左上角显示语言名，Copy 在右上不重叠 | 语言名缺失；或**普通**代码块凭空多出一条顶部空白（`has-lang` 加错了） |

**性能回归怎么看**（`CUSTOM-20260925-043` / `044`）

| # | 怎么试 | ✅ 通过 | ❌ 失败长什么样 |
|---|---|---|---|
| 86 | 长会话（≥100 次工具调用，含子 agent）流式跑起来，看 `ACP Client (Custom)` 输出通道 | 每轮一行 `chat-panel: outbox sent=N merged=M queued=K`，且**每轮从 0 重新计**；`merged` 占比高 | 数字单调递增（`resetStats` 没生效）；或轮末 `queued` 很大（有东西绕过 outbox） |
| 87 | 同上，开 `Developer: Open Webview Developer Tools` | `toolUpdate` 期间没有长任务 | 每次工具更新都全量扫描（`refreshGroupingSoon` 被绕过） |
| 88 | 让 agent 把一个 diff **只改一个字符**（长度不变） | 卡片**会**刷新 | 卡片内容陈旧——这是 040 的自查缺口，044 已修；若复发说明签名又用回了长度 |

**每会话工作目录**（`CUSTOM-20260925-056`..`057`）

| # | 怎么试 | ✅ 通过 | ❌ 失败长什么样 |
|---|---|---|---|
| 90 | 把 `acpc.defaultWorkingDirectory` 设成一个存在的目录（**绝对路径**）→ 新建会话 | 会话头部显示那个目录 | 仍显示工作区目录（说明设置没被读到）；或头部为空（说明传了个不存在的路径） |
| 91 | 把它设成**相对路径**或**不存在的路径** → 新建会话 | 落到工作区目录，且 `ACP Client (Custom)` 里有一行 `ignoring acpc.defaultWorkingDirectory` | 会话起不来（说明坏路径被直接交给了 agent） |
| 92 | 从「Open previous session」点开一条**属于别的目录**的会话（图1 那种跨目录列表） | 记录完整重放；`ACP Traffic` 里 `session/load` 的 `cwd` 是**那条会话自己的**，不是当前工作区 | `cwd` 是当前工作区（说明客户端没回传目录、且本地缓存里也没有它） |

> **这一步的前提条件本身也要能复核**：「会话 cwd 与进程 cwd 不同时，agent 到底在哪干活」
> 由 `CUSTOMIZATIONS/scripts/probe-session-cwd.mjs` 实测（当前结论 `honours-session-cwd`）。
> **协议形状或适配器版本变化后请重跑它**——若结论翻转，整个特性要按「一个 cwd 一个进程」重做。

**草稿页与目录选择**（`CUSTOM-20260925-058`）——**本轮最需要人工确认的一块**

| # | 怎么试 | ✅ 通过 | ❌ 失败长什么样 |
|---|---|---|---|
| 93 | 点标签栏的 `+` | 出现一个 `New session` 草稿页（标签是虚线空心点 + 斜体），**没有**立刻多出会话记录；头部显示目录 | 标签闪一下就没了（草稿被 `sessionsChanged` 冲掉）；或 agent 历史里立刻多一条空会话 |
| 94 | 点头部那个目录 | 拉出抽屉，三组都在：工作区文件夹 / 最近用过 / Browse… | 抽屉不出现；或候选为空 |
| 95 | 选一个工作区文件夹 → 发送 | 新标签出现（不再是草稿）、头部目录是它；**agent 在那个目录里干活** | 目录不对（说明 cwd 没传下去）；或起了两个会话 |
| 96 | 选工作区**之外**的目录（Browse…） | 同上；`Read`/`Bash` 落在那边 | 目录不对（回看 `probe-session-cwd.mjs` 的结论是否还成立） |
| 97 | 在草稿里**先打字再改目录** | 文字不丢；发送用的是最终选中的目录 | 文字被清掉 |
| 98 | 空态点「Connect Claude Code」 | 也是草稿页；agent 起不来时给出明确报错 | 直接建出一个空会话（说明还在走 `connectToAgent`） |
| 99 | 已发出消息的会话，点头部目录 | 抽屉里写明目录已固定；点候选 = **新开一个草稿** | 允许"改"当前会话的目录（协议做不到，是假象） |
| 100 | 键盘：Tab 到目录那格 → Enter → 点一项 | 抽屉可键盘操作、Esc 能关；三个抽屉互不叠加 | 只能鼠标用；或 outline/history/cwd 三个抽屉同时开着 |
| 101 | 点已开始会话的历史列表（↺） | 每行右侧能看到**目录名**（全路径在 tooltip 里） | 只能 hover 才知道哪个项目 |

**客户端模板里写了反引号？**

```bash
node CUSTOMIZATIONS/scripts/check-webview-client.mjs --fix
```

它把**模板体内**（只在模板体内，文件头注释不受影响）的反引号就地换成单引号，然后照常校验。
（这类错误复发过 7 次，修法永远是同一个机械动作——所以做成了工具。`--fix` 是手动用的：
`check-registry.mjs` §5 与 CI 调它时**不带参数**，闸门仍然只报错、不改代码。）

**对话区视觉与可折叠性**（`CUSTOM-20260925-061`..`063`）

| # | 怎么试 | ✅ 通过 | ❌ 失败长什么样 |
|---|---|---|---|
| 102 | 长会话里逐个核对左侧引导条的圆点 | 每个圆点都落在**该条目首行图标的中心**上（工具卡 / Thought / 用户气泡都要看）；竖线两端恰好是首末圆点 | 圆点统一偏上（说明又回到了常量偏移）；或某类条目的圆点对不上（说明锚点退化成了 `firstElementChild`） |
| 103 | 看折叠的 Thought | `💡 ▸ Thought for 3s` —— **三角在图标右侧**；展开后变 `▾` | 三角在图标左侧（说明又用了 `::before`） |
| 104 | 让一个 Thought 从流式走到"收束"（下一片正文到达或轮次结束） | 收束后**图标与三角都还在**，只有标签变成 `Thought for Ns` | 图标或三角消失（说明 summary 重写又变成按名字保留） |
| 105 | 发一条**多行**用户消息（粘贴几行字） | 出现折叠三角；点开/收起正常；**展开时首行不重复显示**；默认是展开的 | 首行出现两遍；或默认是折叠的；或单行消息也长出一个三角 |
| 106 | 发一条**单行**用户消息 | 与改动前完全一样（**没有**三角） | 出现一个没用的三角 |
| 107 | 让 agent 回一个**列很多**的 markdown 表格 | 表格**在气泡内部**横向滚动，气泡宽度不变、面板不出现横向滚动 | 气泡被撑宽 / 整个面板横向滚动（说明 `decorateTables` 没生效） |
| 108 | 滚动长会话，看引导条的"当前"圆点；再按 Tab 走到引导条上 | 当前点**变大**（不是出现焦点色环）；键盘焦点环仍然清晰可辨，两者一眼能分开 | 当前点出现一个与焦点环同色的环（一个颜色两种意思） |
| 109 | 用鼠标点那个 7px 的小圆点 | 明显更容易命中（外观不变） | 还是要瞄得很准 |
| 110 | 开两个会话：在 A 里发消息 → 切到 B → 观察 A 的标签 | A 的标签点变**蓝**（静止，不脉动）；点回 A → 蓝点消失；若 A 仍在流式，则是**脉动的 running 点**而不是蓝点 | 蓝点不出现（说明 `refreshSessions` 的签名漏了 `unread`）；或点了不消失（说明聚焦时没清）；或一个标签同时显示两种状态 |

**记录区：折叠 / 时间 / 工具输出 / 右键菜单**（`CUSTOM-20260925-064`..`068`）

| # | 怎么试 | ✅ 通过 | ❌ 失败长什么样 |
|---|---|---|---|
| 111 | 发一条**长**消息（没有换行、但在面板里折成好几行） | **出现折叠三角**；折叠态是**恰好一行 + 省略号**；展开后文字**连续、无重复、无多余换行** | 没有三角（说明触发判据又只看 `\n` 了——这正是 064 修的）；或展开后半句话处多一个换行（body 变成块级了）；或文字重复两遍 |
| 112 | 发一条**单行短**消息 | 与改动前一样，**没有**三角 | 出现一个点了没用的三角 |
| 113 | 鼠标悬停任意记录 | 出现完整时刻（HH:MM:SS）的 tooltip | 无 tooltip |
| 114 | 让 agent 跑一个耗时命令（几秒） | 跑完后卡头出现时长（`1.2s` / `2m 5s`）；**跑的过程中不显示** | 跑完也没时长（说明 `elapsedMs` 没下发）；或跑的过程中有个数字在跳（那是每帧重渲染） |
| 115 | 点标题栏的「Times」 | 每条记录前出现 HH:MM；再点关闭；**重启面板后状态还在** | 点了没反应；或切换时整个时间线重渲染（卡一下） |
| 116 | 让 agent 回一段命令输出（Bash） | 输出显示为**代码块**（带语言标签与 Copy 按钮）；**不出现字面的 ```** | 仍是纯文本且能看到 ``` |
| 117 | 展开一个工具卡 → 让它继续收到更新 | 已渲染的代码块**不闪回纯文本**（渲染结果被缓存） | 每次更新代码块短暂变回文字 |
| 118 | 在对话区**选中一段文字**后右键 | 菜单只有四项、**没有 Cut/Paste**；`Copy` 可用且真的复制到 | 出现 Cut/Paste；或 Copy 点了没反应 |
| 119 | **不选**任何文字时右键 | `Copy` 是**禁用（灰）**的，`Copy message` / `Select all` 可用 | `Copy` 可点但什么都不发生（那就是原生菜单的老毛病） |
| 120 | 在**输入框**里右键 | 仍弹**系统**菜单（那里 Cut/Paste 是对的） | 被我们的菜单接管了 |
| 121 | 右键菜单打开时按 Esc / 滚动 / 点别处 | 都关掉 | 浮层留在原地 |

**2026-09-26 第二轮（069-075，用户复验 061-068 后的 5 项反馈）**

| # | 操作 | 期望 | 反例（说明哪里坏了） |
|---|---|---|---|
| 122 | 滚到记录区最底部 | 最后一条与输入框之间有约 **24px** 留白 | 仍紧贴输入框上边框（`styles.ts` 的 `.messages` padding-bottom） |
| 123 | 展开/收起**任意**可折叠块（thought、折叠的用户消息、工具卡正文、diff） | 左侧引导条的圆点**立刻**与该条首行重新对齐（收回去也对齐） | 圆点停在展开前的高度（`rail` 的 ResizeObserver 没挂上，或那个节点没被 `watch()`） |
| 124 | 在**窄侧边栏**里发一条 100 字以内、**不含换行**的消息 | 出现 ▸ 折叠三角 | 没有三角（判据又回到字符/换行代理信号了——071） |
| 125 | 在**宽编辑区面板**里发一条足够长但能一行放下的消息 | **不**出现三角 | 出现三角（说明判据是阈值而不是实测行数） |
| 126 | 展开 124 的那条消息 | 原文**连续、无重复、无多余换行**（尤其**没有一整行空行**） | 多一空行（切分点上的 `\n` 没被丢弃）；或文字重复两遍 |
| 127 | 切换模型 / 模式（底部的 ⚡ / ◈ picker） | 记录区出现**居中、左右带虚线**的 `Switched to …`；再切一次**不出现重复的两条** | 一条都没有（`syncChoices` 没被调用）；或每次切换都出现两条（去重失效） |
| 128 | 重开一个已有会话（`session/load`） | **不**出现"切到当前模型"之类的噪声（首次只做基线） | 一打开就打印一条切换提示 |
| 129 | 看一个思考块（thought） | 反引号、`- ` 列表、```围栏都按 markdown 渲染（代码块有语言标签与 Copy 按钮） | 看到**字面**的反引号或 `- `（072 的链路断了） |
| 130 | 观察**正在流式输出**的思考块 | 期间是**纯文本**，定稿那一刻才变成渲染后的形态 | 流式期间文字反复跳变/回退（把某个前缀的 html 应用到了还在长的正文上） |
| 131 | 看任意工具卡头部 | kind 标签（RUN/READ）旁多一个**真实工具名**（Bash / Read / Edit） | 没有名字 chip（`_meta.claudeCode.toolName` 没取到）；或名字把 kind 挤掉了 |
| 132 | 让 agent 给一段带 `- [x]` 的输出 | 有勾选字形（☑/☐） | 勾选态完全消失（白名单 unwrap 掉 `<input>` 的老毛病） |
| 133 | 从树里或让 agent 改一次会话标题 | 出现一条轻提示 | 新会话的**自动标题**也产生了提示（噪声）；或改名后毫无提示 |

> **这一块有一层自动测试**（`src/test/chat-client.test.ts`，桩 DOM）：它测**逻辑**——
> 折叠触发（含"只折行"与"一行放得下"两个方向）、summary/body 的切分、三角与图标的顺序（INV-J）、
> 思考块的 markdown 往返。**布局一律不在其中**（见上面的分区表）：071 之后桩里有一个
> **确定性的"每 N 字一行"模型**，它代替的是"问浏览器占几行"这一步，**不是**在模拟换行算法。

**白条复发时怎么办**（`CUSTOM-20260925-046` 已删除那套常驻诊断）

036/034 装的 `checkTextless` 诊断（每新增条目起 4 秒定时做全量 `getBoundingClientRect` 走查）
已随 046 删除——它的任务是定位白条，而真根因是 **037** 的 `.messages > * { flex: 0 0 auto }`
（flex 子项被压扁，**不是数据问题**）。所以上面「白条诊断（TEMP）」那条验收项已不再适用。

白条若复发，**第一步永远是量几何，不是怀疑数据**（036 的教训：`textContent` 在元素被隐藏/裁剪时
照样有值，用它判断"有没有内容"是错的）。在 Webview DevTools 里跑：

```js
[...document.querySelectorAll('#messages > *')].slice(0, 5).map(n =>
  n.getAttribute('data-kind') + ' h=' + Math.round(n.getBoundingClientRect().height) +
  ' txt=' + String(n.textContent || '').replace(/\s/g, '').length)
```

`txt` 有值而 `h` 是个位数 ⇒ 又是布局/裁剪问题（去看 `.messages` 与 `.messages > *` 的 flex / overflow
规则，并对照 `pitfalls.md` #17）；`txt` 也是 0 ⇒ 才是数据问题。
需要长期诊断时，装一个**只在显式开关下**的轻量版（只测最后 N 条），**不要再让它常驻**。

**面板标题栏**（`CUSTOM-20260924-025`）

| # | 怎么试 | ✅ 通过 | ❌ 失败长什么样 |
|---|---|---|---|
| 48 | 看右侧边栏 chat 面板的标题栏 | 最左侧（`+` 之前）是一个**图标按钮**，与旁边按钮等宽等高；hover 显示 Tip | 出现一个宽的文字按钮（说明命令少了 `contributes.commands[].icon`） |

---

## 常见问题

**窗口没出现 / 很快消失**
`code --version` 是否可用？不可用见上面「方式二」的全路径写法与永久解法。

**改了代码但界面没变**
漏了 `npm run compile`，或忘了在宿主窗口按 `Ctrl+R`。两者都要。

**扩展没激活 / 命令数不对**
命令面板搜 `ACP (Custom)` 应见 21 个。若侧边栏没有图标，确认启动参数里 `--extensionDevelopmentPath`
指向的是**仓库根目录**（不是 `src/`）。

**webview 一片空白**
先看 Webview Developer Tools 的 Console——白屏几乎总是客户端 JS 抛异常，而异常**不会**出现在扩展侧输出通道里。
