---
current_upstream_version: "0.2.0"
current_upstream_commit: "e7371659e3ac100db842b419b1361205a193032e"
custom_version: "0.2.0-custom.2"
last_merge_date: "2026-09-23"
last_release_version: "v0.2.0-custom.2"
last_release_date: "2026-09-24"
vendor_branch: "vendor/main"
upstream_remote: "https://github.com/formulahendry/vscode-acp.git"
---

# ACP Client (Custom) 自定义改动登记

> **本文件是改动登记账本（纯数据）**。机制与规则（冲突策略、标记格式、类型/状态字典、frontmatter 字段职责）见 [`CUSTOMIZATIONS/README.md`](./README.md)。
>
> **只读这一节：「改动总览」**（就在下方）。每个文件一条，多轮演进已合并为当前状态——
> 查"某个文件现在改了什么、上游合并时怎么处理"看它就够了。
>
> **不要读变更日志**：它已拆到 [`docs/changelog.md`](./docs/changelog.md)（42KB，append-only 历史流水）。
> 只在需要追查某次改动的来龙去脉时才去读。
> 拆分原因：原先单体 59KB 而 `AGENTS.md` 把它列为每次任务前必读，等于每次白读 40KB 历史。
>
> **AI Agent 注意**：
> 1. 开发前读**改动总览**定位相关文件与冲突策略
> 2. 完成改动后调用 `acp-record-change` skill：**总览里已有该文件就更新那一节**（合并描述、追加演进链 id），没有就新增一节；同时在 [`docs/changelog.md`](./docs/changelog.md) **顶部追加一条**（注意是两个文件）
> 3. 合并上游时按总览的冲突策略列处理；合并后用 `check-registry.mjs` 复核（它已包含 `acp.*` → `acpc.*` 命名空间扫描，见 §3）
> 4. 不要删除历史（总览条目整体废弃时标 deprecated；changelog 永不删改）
> 5. 一致性自检：`node CUSTOMIZATIONS/scripts/check-registry.mjs`（代码标记 ↔ 总览表双向比对；CI 上也会跑）

---

## 仓库信息

- **上游仓库**：https://github.com/formulahendry/vscode-acp
- **当前基于上游版本**：0.2.0（上游**无 tag**，此值为上游 package.json 的 version，仅表达语义版本）
- **当前基于上游 Commit**：e7371659e3ac100db842b419b1361205a193032e（**唯一定位依据**）
- **当前 Vendor 分支**：vendor/main
- **最近一次上游合并**：2026-09-23
- **最近一次发布版本**：v0.2.0-custom.2（2026-09-24）

---

## 改动总览（按文件）

> **每个文件一条**：该文件当前生效的全部自定义改动（多轮演进已合并）；「标记」列为代码中 `[CUSTOM-BEGIN]` 的 change-id（即该文件自定义区的当前真相），演进链标注历轮 id。代码位置速查见 [`architecture.md`](./architecture.md) §2。
>
> **状态字典**：`active`（生效中）/ `deprecated`（已废弃）/ `merged-upstream`（上游已原生支持）。**冲突策略**字典见 README.md。
>
> **无标记四情形**（详见 README.md）：JSON 无注释 / 测试文件 / 全局性重命名走文件头标记 / "故意保留"说明行必须含 `未改动` 或 `非改动`。

| 文件 | 标记（当前） | 演进链 | 当前效果（合并后） | 冲突策略 | 状态 |
|------|-------------|--------|-------------------|---------|------|
| package.json | （JSON 不支持注释，已知无标记缺口） | 20260923-001→002→014→019→025→030 | ①身份：name `acp-client-custom`、publisher `zouv`、displayName `ACP Client (Custom)`、description 标注 custom fork、repository/bugs/homepage 指向 zouv/custom-vscode-acp，移除上游 `bugs.email`；②命名空间：命令 id → `acpc.*`、视图容器 id → `acp-client-custom`、视图 id → `acpc-sessions`/`acpc-chat`、配置段 → `acpc.agents` 等 4 项、上下文键 → `acpc.turnInProgress`、configuration.title / viewsContainer.title → `ACP Client (Custom)`；③命令 title：**全部 22 个**加 `ACP (Custom): ` 前缀（含上游无 `ACP:` 前缀的 Refresh / Copy Session ID / Forget Session），避免与上游在命令面板与右键菜单中同名；④依赖：移除 `@vscode/extension-telemetry`；⑤**`scripts` 新增 `dev:host`**（→ `node CUSTOMIZATIONS/scripts/dev-host.mjs`，一条命令编译并启动 Extension Development Host）；⑥**019 新增第 22 个命令 `acpc.openChatInEditor`**（title `ACP (Custom): Open Chat in Editor Area`，**025 起带 `"icon": "$(open-preview)"`**）+ `view/title` 的 `view == acpc-chat` 项（group `navigation@0`）+ 快捷键 `ctrl+alt+a` / `mac: cmd+alt+a`；**030 又给它加了 `acpc-sessions` 视图的标题栏按钮**（`group navigation@0`，侧边栏最上方常驻一个入口）；**刻意不加** `editor/title` 菜单（其 `when` 需要 `activeWebviewPanelId` 上下文键，本仓库从未验证过该键——`acpc.turnInProgress` 就是"声明了却没 setter"的前车之鉴）；⑦**030 `acpc.agents` 默认列表把 `Claude Code` 提到第一位**（原来在 `GitHub Copilot` 之后）。**合并上游时注意**：`scripts` 块与 `acpc.agents` 默认列表是本文件里最可能冲突的区域 | merge-manual | active |
| package-lock.json | （锁文件，无标记） | 20260923-002 | 移除 `@vscode/extension-telemetry` 后由 `npm install` 重新生成 | 接受上游版本后 `npm install` 重新生成 | active |
| .vscodeignore | 20260923-003 | 20260923-003 | 追加排除 `CUSTOMIZATIONS/**`、`.agents/**`、`.claude/**`、`AGENTS.md`、`release/**`、`.github/**`，防止开发机制文件与 CI 配置被打进 `.vsix` | merge-manual | active |
| .gitignore | 20260923-003 | 20260923-003 | 追加忽略 `release/`、`.zcode/plans/`、`.claude/explore-results/` | merge-manual | active |
| src/extension.ts | 20260923-001/002/010/011/012/019/020 | 20260923-001→002→010→011→012→019→020 | ①文件头标记：本文件参与全局命名空间重命名（`createTreeView('acpc-sessions')`、`registerCommand('acpc.*')`、`executeCommand('acpc-chat.focus')`、`getConfiguration('acpc')` ×2）；②移除 `extension/activated` 遥测上报（原实现读取 `formulahendry.acp-client` 的版本号，fork 后该 id 不存在）；③**多会话改造**：事件接线改为 session 作用域——所有转发到聊天面板的事件（`clear-chat`/`mode-changed`/`model-changed`/`session-load-start`/`session-load-end`/`session-info-changed`）统一以 `getActiveSessionId()`（聚焦会话）过滤，否则后台会话的更新会打到前台面板上；`connectAgent` 的切换确认文案由「将断开 X 并清空历史」改为「X 在后台保持连接」；④**面板路由**：`registerWebviewViewProvider` 的 provider 由 `ChatWebviewProvider` 换成 `ChatRouterProvider`（视图 id 仍是唯一的 `acpc-chat`）；`active-session-changed` 的订阅从本文件移到路由层（焦点变化正是决定哪个面板接管视图的依据）；⑤**确认文案修正**：`newConversation` / `openSession` 两处由「会清空/替换聊天记录」改为「当前会话保留在它的标签中」——多会话下新建只是开新标签，原文案与实现不符；⑥**019 新增命令 `acpc.openChatInEditor`**（第 22 个，标记块 `CUSTOM-20260924-019`），转调 `chatRouter.openEditorChat()`；⑦**020 权限桥接线**：`permissionBridge` → `ConnectionManager` → `SessionManager.setPermissionBridge` / `setSessionLookup` / `session-closed → cancelSession` → `ChatRouterProvider`；dispose 链先 `cancelAll()` 再拆面板；⑧**033 `openSession` 命令改用 `SessionManager.openExistingSession`**——load/resume 的决策收敛到一处（原来命令里内联判断 caps，与面板历史选择器各持一份）；进度提示文案由 `Loading session…` 改为 `Opening session…`（resume 路径现在也走它），`resumed` 的提示改由返回值决定 | merge-manual | active |
| src/ui/SessionTreeProvider.ts | 20260923-001 | 20260923-001 | 文件头标记：树项命令 id 全部改为 `acpc.*`（`acpc.openChat` / `acpc.openSession` / `acpc.loadMoreSessions` / `acpc.connectAgent` / `acpc.refreshSessions`） | merge-manual | active |
| src/ui/ChatWebviewProvider.ts | 20260923-001 | 20260923-001 | 文件头标记：`viewType` 改为 `acpc-chat`；内联 HTML 里 welcome 按钮的 `executeCommand('acpc.connectAgent')` / `('acpc.addAgent')`。**CUSTOM-20260923-011 起它成为 legacy 面板，被 LegacyPanelAdapter 用 facade 包装后接入路由层——本文件本身零改动**，旧缺陷也不在此文件内修（见该轮日志的「非目标」） | merge-manual | active |
| src/ui/chat/（新 Chat 面板与路由层，全部为新增文件：`index.ts`、`ChatRouterProvider.ts`、`ChatPanelHost.ts`、`ChatSurface.ts`、`ChatEditorPanel.ts`、`Outbox.ts`、`LegacyPanelAdapter.ts`、`panelContract.ts`、`protocol.ts`、`markdown.ts`、`sessionChoices.ts`、`transcript/`、`content/`、`nesting/`、`html/`） | 20260923-011/012/013/017/018/019/020/021/022/023/024/026/027/028/029/031/032/033/034/035/036/037/038/039/040/041/043/044/045/046/047/048/049/050/051/052/053/055/057/058/061/062/063/064/065/066/067/069/070/071/072/073/074/075/076/077 | 20260923-011→012→013→017→018→019→020→021→022→023→024→026→027→028→029→031→032→033→034→035→036→037→038→039→040→041→043→044→045→046→047→048→049→050→051→052→053→055→057→058→061→062→063→064→065→066→067→069→070→071→072→073→074→075→076→077 | 上游**没有**这些文件，因此不会产生合并冲突（`git merge vendor/main` 只触碰上游改过的路径）。用途：①单视图路由层按聚焦 agent 分发新/旧面板；②扩展侧 `TranscriptStore` 支撑多会话标签页与「切面板不丢内容」；③`ToolInvocationStore` 实现 ACP 的 replace-collection 语义；④`SafeMarkdown` 零依赖 markdown 消毒；⑤`html/` 下把 webview 的 HTML/CSS/JS 拆成可维护模块，编译期拼接（无新构建步骤）；⑥`nesting/` 子 agent 分组推断（**ACP 无嵌套概念，全部是启发式**，UI 上标注为推断）。**012 修复了首次审计出的 15 个缺陷**（详见该轮日志）。**013 新增 `nesting/`**。**017 修复阻断级 bug**：`hidden` 属性被 `styles.ts` 里的 `display: flex` 覆盖导致加载遮罩常驻（并吞掉全面板点击），加一条全局 `[hidden]{display:none!important}` 修好（详见该轮日志）。**018 块视觉体系**：推理块改为"轻卡片"（折叠态安静一行 / 展开态淡背景卡片）、渲染时自动折叠、三档间距阶梯（同类紧/跨类松/轮次边界最松）。**019 双 surface**：新增 `ChatSurface.ts`（把 `WebviewView`/`WebviewPanel` 归一化：显隐、前置 `show` vs `reveal`、销毁）与 `ChatEditorPanel.ts`（编辑区面板，视图类型 `acpc-chat-editor`）；`ChatPanelHost` 由单 `view` 字段改为 `surfaces` Map，`post()` 默认**广播**、`boot` 只回发起者（广播 boot 会让另一个文档反复 hydrate 并丢滚动位置）；不变量「编辑区面存在 ⟺ 聚焦 agent 属于 `MODERN_AGENTS`」由 `open()` 拒绝 + `ChatRouterProvider.syncActivePanel` 关闭双向保证。**020 面板内权限卡**：transcript 新增第 8 种记录类型 `permission`（`PermissionState` 状态机 `pending`/`deferred`/`selected`/`cancelled`），新增客户端模块 `html/client/permissionView.ts`（**禁用 innerHTML**——标题与选项名都是 agent 可控字符串）；`ChatPanelHost` 实现 `PermissionPresenter`（`canPresent` 要求「聚焦会话 + modern agent + 有 surface」，surface 不可见时 `reveal(true)` 但不抢焦点）；协议新增会话作用域的 `permissionAnswer`（必须放在 `verifySession` 守卫**之后**）；最后一条 surface 断开时调 `bridge.onPresenterLost()` 把待决请求转成弹框。**021 会话大纲抽屉**：纯客户端（不动协议/命令/package.json）——新增 `html/client/outline.ts`，标题栏 ☰ 拉出用户消息列表（首行 ≤80 字 + HH:MM），点击跳转、当前项高亮；`transcriptView` 新增显式 `order` 数组与 `ordered()/entry()/node()` 访问器（**不要用 `Object.keys` 的顺序**），并在 `place()` 里给每个节点打 `data-entry-id`；`dom.ts` 新增 `NS.dom.schedule`（一帧一次的回调队列，读写分离）；`scroll.jumpTo(node)` 在写 `scrollTop` **之前**置 `pinned = false`，否则流式分片会把视口拽回底部。**022 长会话性能**：新增 `Outbox.ts`（合帧队列，**只合并队尾单条同 id 的 append**——`TranscriptStore` 是就地修改并返回同一对象，所以攒着不发不会丢内容）；`ChatPanelHost.post()` 改为**按消息类型路由**（`STRUCTURAL_MESSAGE_TYPES` 立即发，其余入队；路由表集中在一处，漏改一个调用点会静默破坏 INV-A/INV-C）；`refreshSessions()` 加**签名去重**（它每个 chunk 都被调用一次，此前每片都重建整条标签栏）；客户端 `setStreamingText` 改为**增量追加**（新文本是旧文本前缀时 `appendData`，否则回落整段重设）;`NS.dom.schedule` 一帧一次的回调队列扩到滚动与工具分组重排；滚动记忆改成 `{top, pinned}` + `reassert()`（markdown 回填后重新对齐）+ `persistUi` 持久化。**INV-A..F 全文见 [`docs/arch/chat-panel.md`](./docs/arch/chat-panel.md) §5.10，改这块前必读**。**023 左侧引导条**：新增客户端模块 `html/client/rail.ts`（两级：轮次大点 + 步骤小点，**随内容滚动**、可点击跳转、当前项高亮、工具失败/进行中分别着色），与 021 的 ☰ 抽屉**职责并存**（抽屉管跨轮次回看，引导条管本轮进度）；`body.ts` 加 `#rail/#railTrack`（`.message-area` 的**兄弟**节点，不放进 `#messages`——那里的三档间距阶梯 `.messages > * + *` 会把它当条目位移）；`.messages` 的 `padding-left` 增到 19px 给引导条留通道；`transcriptView.updateTool` 顺带把新 view model 存回 `objects[]`（引导条读 `status` 着色，而 status 不在 `PATCH_KEYS` 里）。**024 修「+ 按钮点了没反应」**：`newSession` 原先调 `connectToAgent`，而它的语义是**已有会话就复用**（只 `focusSession`），于是「开新会话」在有会话时退化成「聚焦已有会话」；改为调 `newConversation` → `createSession`。同时修掉客户端 `tabs.ts` 里 `if (state.focusedAgent)` 造成空焦点时点击被**静默吞掉**（退回到 `sessions[0].agentName`；连会话都没有时不带 agentName 上报，由扩展侧回一条明确提示）。**026 修「对话里成串的空白条」**：两个独立成因——① `agent_message_chunk` 的**空文本块**（agent 拿它当分片分隔符）被 `toContentView` 映射成合法的空文本视图，建出 `content` 记录 → 无边框细白条；② 工具调用后的纯空白分片让 `appendAssistantChunk` **新建**了一条空气泡（推理块同理，表现为一个没有时长的 `Thought`）。修法：`contentBlocks.ts` 新增 `isBlankText`（**按码点**排零宽字符，不用字符类正则——见 pitfalls #12 复发记录）与 `hasVisibleContent`；`postContentNotice` 与 `append{Assistant,Thought}Chunk` 各自加「看不见就不建记录」的守卫（合并进已有流式气泡仍然照旧，段落换行不受影响）；客户端 `toolCallView.isBlank` + `buildContent` 做兜底。**027 修「工具卡只有空框」**：018 只让 **append 路径**能重建空壳，而 `tool_call_update` 走的 **`updateTool` 仍然只能打补丁**（`toolCallView.update` 只改 status/kind/title/body 三个 span，永远修不了一个没有 `.tool-head` 的空壳）——所以卡片第一次落地时若没带上 view model，空壳就永久留下。三层修法：① `updateTool` 加同样的空壳重建分支（补上 018 漏掉的那条路径）；② 客户端新增会话作用域消息 `needToolView`，`place()` 遇到没有 view model 的工具条目时**不再渲染空壳**，改为渲染一张由 `toolCallId` 拼出的最简卡片并**向扩展侧要一次**视图模型（`resendToolView` 定向回该面；调用不在索引里则记日志说明「不是客户端丢的」）；③ `toolCallView.update` 补上 `.tool-kind` 的补丁（占位卡的 "Tool" 要能变成真实 kind）。**028 记录类型图标**：新增客户端模块 `html/client/icons.ts`（内联 SVG，`stroke=currentColor` 自动跟随主题；**不用 codicon 字体**——那要改 CSP 加 `font-src` 并随扩展打包字体），用户/助手/推理/计划/通知五类挂在条目上，工具卡图标在卡头内由 `toolCallView.render` 建。挂载点是**按"谁会被重写"选的**：助手气泡的文本会被流式整段重写、markdown 落地还会 innerHTML 覆盖 → 图标挂**条目外层**（配 `.entry-assistant` 的 row 布局）；推理块的 body 每片重写 → 图标挂 `summary`（收束时那次重写已改成保留图标）；工具卡头从不整块清空 → 挂卡头；`content` 与 `permission` 不加（前者已有图片/chip 形态，后者自带 `?` 角标）。**029 图标挂载点改回气泡内 + 日志桥**：028 为躲开"文本被整段重写"曾把助手图标挂到**条目外层**并加 `.entry-assistant` row 布局——那是 028 里唯一影响全局排版的改动，已撤销；现在图标在气泡内，由两条写文本的路径（`setStreamingText` 的整段替换、`setSanitizedHtml` 的 markdown 落地）用 `takeIcon`/`putIcon` 保留并还原，**不再有任何布局改动**。同时新增 webview → 输出通道的**日志桥** `clientLog`（`console.warn/error` 与未捕获错误转发到 `ACP Client (Custom)`，上限 50 条）——webview 控制台不开 DevTools 看不见，这个盲区已经让两次排查卡住。**031 编辑区面板不再依赖侧边栏**：`ChatEditorPanel.open()` 的门禁由「聚焦 agent 必须是 modern」放宽为「**只有**存在聚焦 agent 且它是 legacy 时才拒绝」——**没有聚焦会话是合法状态**（窗口重载后 `activeSessionId` 就是 null，而状态栏显示的是 `activeSession?.agentDisplayName || connectedAgents[0]`，会让人误以为"明明就是 Claude Code"）；`syncActivePanel()` 的关闭条件同步改成「聚焦到 legacy 才关」，否则刚打开就会被下一次 `active-session-changed` 关掉；无聚焦会话时 `focusMostRecentModernSession()` 复用面板 agent 选择器那套（取最近一个 live 会话并 `focusSession`），打开即有内容，**刻意不自动建会话**（`open()` 不该 spawn 进程）。**032/033 面板内的两个新入口**：① 空态「Connect Claude Code」按钮（`connectAgent` 消息 → `connectToAgent`：已有会话就聚焦、没有就建一个）；② 标题栏 ↺ 历史会话选择器（新增客户端模块 `html/client/sessionMenu.ts`，复用 `.outline*` 样式；`listHistory` → `history` 消息，`openHistorySession` → `SessionManager.openExistingSession`）。**三条消息都刻意不带 sessionId**（要用它们时往往没有聚焦会话），因此都在 `verifySession` 守卫**之前**处理。**034** 把"白条"TEMP 诊断从只在 `hydrate` 跑，扩到 `markdownRendered` 与 append 的 4 秒防抖——hydrate 只在切会话/attach 时发生，**盯着一个实时会话的人永远不会触发它**（这正是上一轮用户查不到那行日志的原因）。**038-041 面板审计阶段 1（正确性，保守档）**：一次系统性审计（逻辑/性能/交互/无障碍四面的结论见 [`docs/arch/chat-panel.md`](./docs/arch/chat-panel.md) §5.12）后落地的四条修复。**038** 修「plan 卡永不更新」：协议层的 `EntryPatch.plan` / `.content` 与**记录层的字段名** `entries` / `blocks` 不一致，客户端 `PATCH_KEYS` 照抄了协议层的名字 ⇒ patch 写进 `entry.plan` 而 `buildPlan()` 读 `entry.entries`，**更新被静默丢弃**（`patch()` 对不认识的键不报错）；表现是 Claude Code 的 Todo 列表永远停在第一次快照、切走再切回才对。四处统一到记录字段名（`types.ts` / `TranscriptStore.patch` / `ChatPanelHost` 的 plan 分支 / 客户端 `PATCH_KEYS`），`content` 那条是潜伏缺陷一并改掉；**这类错位 tsc/lint/webpack 全看不见**，已回写 pitfalls。**039** 修 `resource_link` chip：此前一律写 `data-href` → 落到 `openLink` → 被 [`docs/arch/chat-panel.md`](./docs/arch/chat-panel.md) §5.5 的协议白名单拦下，点本地文件只弹 `Blocked link with unsupported scheme: file`；改为在宿主侧 `contentBlocks.localPathOf()` 判定（`file:` 走 `node:url` 的 `fileURLToPath`，裸路径/相对路径原样，**Windows 盘符必须先于 scheme 判定**），客户端按 `path` / 可放行协议 / 两者都不是，分别渲染 `data-path` chip / `data-href` chip / **静态 chip**（新增第三态：没有可用打开方式就不该长成可点的样子）。顺带把 `handleOpenFile` 解析相对路径的依据从 `this.focused.sessionId` 改为守卫校验过的消息 sessionId。**040** 修工具卡两处同根缺陷（`render()` 与 `update()` **各写了一遍 body 构建**）：① `locations` chips 在 update 路径既不会补上晚到的、还会被 `clear(body)` 清掉；② `renderDiff` 每次以 `body.hidden = true` 新建 ⇒ 用户展开的 diff 被下一次更新合上。抽出 `fillToolBody()` 单点构建 + `captureExpansion`/`applyExpansion` 按 `data-expand-key` 搬运展开态；并加 `bodySignature` 跳过「只改了 head 里 status」的无谓重建（顺带省掉重复 LCS）——签名**刻意不序列化 payload**，遇 image block 返回 `null` 表示不可缓存、宁可多建一次。**041** 补 `ChatPanelHost.dispose()` 漏掉的 `sessionUpdateHandler.removeListener`（legacy provider 是注销的，此前只是碰巧被 `SessionUpdateHandler.dispose()` 兜住），并把 `Outbox.stats()` 从累计改为**单轮**（新增 `resetStats()`，`finalizeTurn` 打印后清零）——累计会让 `merged/sent` 漂向长期均值，那行诊断随时间失去意义。**043-052 面板审计阶段 2-4（性能 / 交互无障碍 / 清理）**：阶段 1 的审计结论（[`docs/arch/chat-panel.md`](./docs/arch/chat-panel.md) §5.12）里那些「查到但没修」的问题，本轮全部落地。**性能**：**043** 嵌套推断去重复序列化——`payloadContains` 曾对每个候选父调用 `JSON.stringify` 其 `rawOutput`/`_meta`（上限 200KB），而 `apply()` 在每次 `tool_call_update` 上全量重算，于是宿主聊天链路最贵的事是反复序列化同一个没变的对象；改为按**载荷对象本身**缓存的 WeakMap（存已切到 `MAX_SCAN_CHARS` 的串，语义不变、内存有界），安全前提是 `upsert*` **整体替换**字段而非就地改（引用变化即失效信号）；`isTaskLike` 同样备忘；`ToolInvocationStore.list()` 去掉 `.sort`（`Map` 本就是插入序、`startedAt` 插入时打戳不改，两种序按构造同一个，那个排序是每次通知都付的 O(n log n) 空转）。**刻意不加每会话容量上限**（原计划有一条）：`snapshotOf` 从本索引补水视图模型，淘汰一条记录仍在 transcript 里的调用正好会渲染出 027 消灭掉的"空壳卡片"，安全上限需要从宿主推入引用计数（与 `setLiveSessionIds` 对称），是独立改动——理由写在类注释里。**044** 工具路径并入 rAF 合帧（`update()` 末尾原本**同步**跑整条 transcript 的 `querySelectorAll`，而命令每输出一片就推一次更新；合帧器搬进拥有 `refreshGrouping` 的 `toolCallView`，两条路径共用一份），**并补掉 040 自查出的陈旧渲染缺口**：`bodySignature` 原用**字符串长度**判变化，同长度改写会跳过重建、把旧内容永久留在屏幕上——改为 FNV-1a 哈希（256K 以内 O(n)，超阈值返回 `null` 走 040 已建立的"无条件重建"逃生门，语义永不退化）。**045** 抽屉的"要不要显示 ☰"从每次 append/revise 全量扫描降到 O(1)（`transcriptView` 维护 user 条目计数器，`place()` 是唯一新增入口）。**046** 删掉常驻生产路径的 TEMP 诊断（`checkTextless` 等约 98 行：每新增一条记录起 4 秒定时做**全量 `getBoundingClientRect` 走查**，未命中还会反复测量）——白条真根因 037 已修，这套只剩成本。**交互与无障碍**：**047** 键盘可达性——标签/抽屉行/菜单项/斜杠项/工具卡头/diff 头/chip/引导条点全部从 `div`/`span` 换成真 `<button type="button">`，**Enter/Space 由浏览器原生触发 click**，所以委托式 click 处理器一行未改就同时支持键盘（比补 keydown 分支更不容易漏）；CSS 加一小块"抹平 UA 按钮样式"的基础设施（**必须放在各 class 规则之前**才能保持外观一致）+ `:focus-visible` 焦点环（面板此前完全没有焦点样式）；标签栏补 `aria-selected` + roving tabindex + ←/→ 切换 + Delete 关闭，引导条同样 roving tabindex（标记可能几百个，逐个进 tab 序列会让 Tab 彻底失效）；`aria-expanded` 跟着卡头展开态；接上从未被调用的 `focusInput()`（文档获焦且用户未在选字时把光标放进输入框——否则 `acpc-chat.focus` 键绑定只完成一半）；打不开的 `resource_link` 渲染成 `<span>` 而非 `<button>`（不能点就不该占 tab 停靠位）。**048** 流式播报：`#messages` 加 `role="log"` + `aria-live` + `aria-relevant="additions"`，并由 `transcriptView` 用**计数器**驱动 `aria-busy`（有任一条在流式就 busy，辅助技术推迟播报、结算后整段念一次）——直接加 `aria-live` 会让读屏把逐片重写的回答从头念几十遍。**049** 附件拖拽：新增**会话作用域**消息 `attachPath`（守卫**之后**处理）；`text/uri-list`（VS Code 内部拖拽，主用例）+ `files[].path` 两级取路径，**两者都拿不到时明确发 error 通知**而不是静默；宿主侧把"加入待发附件 + 广播"抽成 `addAttachments` 共用，**拖拽路径刻意不 reveal 任何 surface**（拖拽本就发生在可见的面上，再 raise 等于抢焦点）；粘贴图片暂不支持（剪贴板图是 Blob、无路径，落盘需要受管附件目录 + 清理策略，属另一功能），给明确提示。**050** 按会话记忆草稿（原先切会话 `input.value = ''`，切去另一标签看一眼就丢掉刚打的字；复用滚动记忆那套 `persistUi`；**`stashDraft()` 必须在 `state.sessionId` 改写之前调用**，否则退场会话的草稿会记到入场会话名下；会话关闭时 `forgetDraft`）；编辑区面改用编辑器底色（`body(surface)` 写成 `<body class="surface-view|surface-editor">`，`renderChatHtml` 多一个默认 `'view'` 的形参，侧边栏路径一行未改；CSS 里**只有** `.surface-editor` 的覆盖规则）；`prefers-reduced-motion` 关掉 pulse/spin。**051** 代码块显示围栏语言标签（`SafeMarkdown.code` 早在输出 `data-lang`，界面从未用过；标签放左上角、`pointer-events:none`，只有带语言的块才让出顶部内边距；**刻意不引入高亮器**——那要加依赖并改 CSP）。**052** 清理零引用死代码（`dom.esc`+`ESCAPES`、`toContentViews`、`TranscriptStore` 的 `sessionHasContent`/`listSessionIds`/`size`/`entriesNeedingMarkdown`、`ToolInvocationStore.dropAgentSessions`）——每处都先 grep 复核；`esc` 删除处留了"**不要补回来**"的说明（通用转义助手是"用字符串拼 HTML"的邀请函，而客户端每次插入都走 `el`/`textContent`/`setSanitizedHtml`）；`toolCalls.ts` 的 `statusGlyph`/`statusClass`/`kindLabel` **保留**（宿主侧拼写，将来日志/树项会要）但加注释「**改一处必须改另一处**」并点明这正是 038 那类漂移的温床；`styles.ts` 的 `.tab-dot.attention` **保留**并注明「设计预留、从未接线，看到没生效别当 bug 修」。**本轮共 3 处自查出的缺口**（040 的长度签名、049 的正则转义、以及各条被 `check-webview-client.mjs` 抓到的模板反引号），全部回写。**053/055 replay 路径收尾与「重开后正文消失」**（用户报「重新打开看到的会话跟实时看到的不一样」）。**055 是这一轮的真根因**：`contentBlocks.isBlankText()` 的 `return true` / `return false` **写反了**——它的名字、文档注释、以及**客户端的同名副本**三者都对，只有函数体是反的（`isBlankText('abc')` 返回 true、`isBlankText('   ')` 返回 false）。调用点语义是「看不见就不建记录」，反过来就成了**「看得见就不建记录」**：每个*新开一条记录*的真切正文分片被丢掉、空白分片反而保留。**为什么藏了一整天（026→055）而所有自动检查全绿**：**实时路径靠巧合是对的**——实时流里 agent 先发一个换行/空白分片，按反了的判据它"不是空白"⇒ 记录被建出来，之后真切正文走**合并**分支（`merges === true` 时守卫被整段跳过）⇒ 正常追加；而 `session/load` 的 replay 是**单块真切正文、前面没有空白分片** ⇒ 记录直接丢。这正是"实时好、重开坏"的全部原因（顺带说明：`''` 走的是 `if (!text) return true;` 提前返回，那条是对的，所以 026 针对"空文本块"的半修法确实生效，针对"纯空白分片"的半修法**从未生效**）。修法是把两处 `return` 对调。**同时给 `session-load-end` 补上收尾**：replay 是有限的流，结束后不可能还有东西在流式，而没有任何东西会关闭它建出来的记录（`finalizeTurn` 只为我们自己发的 prompt 跑）；不补的话最后那条助手记录永远停在 `streaming: true`，把 `aria-busy` 钉在 true、**读屏从此静默**。**053** 补上 `user_message_chunk` 处的 `finalizeEntries({only:'assistant'})`（legacy 面板一直这么做），解决 replay 中"没有工具调用跟随的助手记录永远流式"这一类；**诚实说明：053 不是用户报的症状的原因**（`streaming: true` 的气泡照常显示文本），它是在排查中顺带读出来的独立缺陷。**055 的复现与验证靠 054 建的测试**（见该行）。**057 每会话工作目录**：`protocol.ts` 的 `openHistorySession` 增加可选 `cwd`；`ChatPanelHost.handleOpenHistorySession` 把它一路传给 `openExistingSession`；`html/client/sessionMenu.ts` 在行上写 `data-open-cwd` 并在点击时回传。（「草稿页 + 目录选择器」是下一步 058，会加 `listDirectoryChoices`/`pickDirectory`/`createDraftAndSend` 与 `draftResolved`/`draftFailed`，都按 §5.4 规则放在 `verifySession` 守卫**之前**。）**058 草稿页 + 目录选择器**：`protocol.ts` 新增 3 条 webview→ext（`listDirectoryChoices` / `pickDirectory` / `createDraftAndSend`）与 4 条 ext→webview（`directoryChoices` / `directoryPicked` / `draftResolved` / `draftFailed`）——**都是非会话作用域**（草稿按定义没有 sessionId），因此在 `verifySession` 守卫**之前**处理；四条应答都**定向**发给发起请求的那个面（那是它自己在编辑的草稿）。**草稿是纯客户端状态**（`boot.ts` 的 `drafts`/`focusedDraftId`）：点 `+` 只开本地标签、不发建会话请求，`sessionsChanged`/`boot` 都不许把它冲掉（`tabs.setSessions` 刻意不碰 drafts）。发出第一条时才 `createSession(agent,{cwd,focus})` → **复用 `handleSendPrompt`**；**不做"先建再重建"**，因为 `session/close` 不会把会话从 agent 历史里移除（图1 两条空会话是证据）。失败时草稿与已输入文字都保留（composer 在草稿模式下**刻意不清空输入框**）。目录候选三组（工作区文件夹含多根 / 最近用过（本地缓存与 agent 侧 `session/list` 合并）/ 默认）+ 「浏览…」走宿主 `showOpenDialog`。新增 `html/client/directoryMenu.ts`（抽屉形态照抄 `sessionMenu`，三个抽屉互斥）；头部显示目录的 `#sessionTitle` **从 span 变成真 `<button id="cwdBtn">`**（延续 047）。已开始的会话目录只读（协议固定在创建时），抽屉里点候选 = 在新目录里开新草稿。空态 Connect 改为 `ensureConnected` + 草稿（只拉进程、不建会话）。历史行加 `.outline-cwd` 显示所属目录。**061-063 对话区视觉与可测性**（用户 F5 复验截图的三条反馈 + "总体盘一下"评审出的三处缺陷）。**061**：①**引导条圆点对齐**——`rail.measure()` 原本是 `top = y + 6`，而 CSS 用 `margin-top: -半高`抵消尺寸 ⇒ `top` 就是圆心，于是圆心被钉在"条目顶边 + 6px"，而工具卡（3px 内边距）与用户气泡（6px）的首行中心并不在那儿，**统一偏上 6–9px**；改为**锚到条目里的类型图标**（`icons.attach()` 本来就把它放在首行上，其几何中心就是要对的中心）——**按构造对齐，不再靠常量**；`points[]` 的 `y`（条目顶边，供 `syncActive` 二分）与 `centre`（圆心）分开，`rail-line` 两端改用 `centre`。②**Thought 三角移到图标右侧**：三角原是 `summary::before`，而伪元素永远画在内容之前、图标又是 prepend ⇒ 顺序成了「▸ 图标 标签」；改为真元素 `.fold-caret`（字形与方向仍由 CSS 依 `details[open]` 驱动，**切换不需要 JS**），并同步改 `patch()` 的 summary 重写——它原本只保留 `.rec-icon`，会把新三角清掉；现在**保留所有元素子节点、只丢文本与 `.thought-spin`**（"保留元素"不会像名字清单那样漏掉新挂件）。③**用户消息折叠**（与 Thought 同一形态）：多行消息包成 `<details class="user-fold" open>`，**首行在 summary、其余行在 body**（刻意不重复全文），单行消息**不出现三角**（没有可折的东西），**默认展开**（刚发的消息不该被藏起来）。**062** 三处缺陷：①宽 markdown 表格横向撑破气泡（`.bubble pre` 有 `overflow-x` 而 `.bubble table` 没有）→ 在 `links.ts` 加 `decorateTables()` 并与 `decorateCodeBlocks()` 合成**单一入口** `decorateScrollables()`（将来新增块类型不会只记得一处）；②引导条"当前位置"的环用的是 `--vscode-focusBorder`，而 047 刚把该变量定义为**键盘焦点**语义 ⇒ 一个颜色两种意思，改用**尺寸**（`transform: scale(1.4)`，以中心为基准所以不动对齐），**刻意不用环**；③7px 圆点鼠标难点中 → `.rail-dot::after { inset: -5px }` 把命中区扩到约 17px，**外观不变**。**063** 把闲置的 `.tab-dot.attention` 接上线：**非聚焦会话产生了输出**就在标签上点一个蓝点（`ChatPanelHost` 的 `unread: Set` + `toSummary` 带 `unread`）；**只在一个咽喉点标记**——所有带记录的下行消息都经过 `post()`，在那里按 `type ∈ {append,revise,toolUpdate}` 且非聚焦打标（每条调用点各写一遍迟早会漏，同 040 的教训）；**`refreshSessions()` 的签名必须包含 `unread`，漏了标签永远不刷新**（022 签名去重的既有陷阱），并在"新标记产生"与"聚焦清除"两处各主动刷新一次（有些 append 路径本来就不会自己刷）。优先级 `loading > running > unread`（流式本来就在脉动）。**它只是提示**：不弹窗、不抢焦点。**064-067 记录区**（用户 F5 复验 061/063 的反馈 + "总体盘一下"的评审）。**064 修「多行用户消息也不折叠」**：判据原本只看 `
`，而"多行"有**两种**含义——**逻辑换行**与**长句子视觉折行**，后者一个换行符都没有 ⇒ 长消息完全没折叠（用户报的正是它）。改为「有换行 **或** 长度 > 约 160 字」；切分点优先取换行、否则取 160 字内最后一个空格；折叠态 summary 占**恰好一行**并省略号截断（**只在折叠态**——展开时还 nowrap 会把第一段截成"半句话加省略号"），展开态 body 用 **inline** 以免在半句话处多出换行。**定位方式是本轮方法论的关键**：读代码怎么看都对，于是一个**桩 DOM** 把客户端逻辑真跑一遍，三种输入一次定性。**065 时间信息**：先说事实——**每条记录都已带 `at`**，只是从没显示过（Thought 的 `Thought for Ns`是耗时不是时刻）。三个层次：**hover** 看完整时刻（零成本）；**工具耗时**（`ToolCallView.elapsedMs`由宿主 `endedAt - startedAt` 算，卡头常显，与 Thought 同类信息；**不下发两个时间戳**，仍在跑的不显示以免每帧重渲染）；**头部「Times」开关**（`persistUi` 持久化）⇒ `.rec-time` 元素始终在 DOM、可见性由类决定 ⇒ **切换不重渲染**。时长格式化放 `NS.dom.duration`（模块加载顺序只允许依赖指向前方）。**066 工具输出按 markdown 渲染**：抓包证据——**12 处工具结果文本块带 ``` 围栏**（Claude Code 的 Bash结果本身就是 markdown 包装的），按纯文本渲染于是显示字面围栏、丢掉代码块样式与 Copy。修法是**复用助手气泡那条往返**（不写第二个更弱的渲染器），给工具内容项一个稳定 key（`entryId#下标`），带 key 的项宿主不做 `transcripts.patch`；客户端两份缓存按会话清空（body 在 `bodySignature` 变化时会整块重建，缓存是 HTML 挺过重建的关键）。顺带把 `post`/`postNow`/`send` **重复三遍**的 `markdownRendered` 内联类型收敛成 `protocol.MarkdownRendered`（加一个字段要改三处 = 漏一处就静默）。**067 自定义右键菜单**：取代 Chromium 原生菜单——它的 `Copy` 复制的是**选区**（右键时通常没有选区）⇒ "点了没反应"；`Cut`/`Paste` 对只读内容无意义。改为按上下文给 `Copy`（**无选区时禁用**，禁用项比没反应的项诚实）/ `Copy message` / `Copy code` / `Select all`，**没有 Cut/Paste**；复制走扩展侧 `copy` 通道；输入框里不接管。**068 客户端逻辑的常驻测试**（`src/test/chat-client.test.ts`，桩 DOM）：只加载被测链路（dom → icons → links → toolCallView → transcriptView），其余给最小替身；**能测逻辑、不能测布局**（尺寸/换行/颜色/焦点需要真 Chromium）。四条断言：单行不折 / 多行折叠且summary+body **原样重建**原文 / **长单行也折**（064）/ **INV-J**（三角在图标后、重写后都在）。每个文件都带 `[CUSTOM-BEGIN]` 头标记以便追溯。**069-075 第二轮用户反馈（用户 F5 复验 061-068 的 5 项）**：**069** 记录区底部留白 4→24px（最后一条贴着输入框上边框时，读者会以为"下面还有、没滚到底"）；**070** 引导线改用**一个 ResizeObserver 观察每条记录节点**来重对齐——原先只在"标记集变化 / window resize / markdown 回填"三处测量，展开 `<details>`、展开工具卡正文、图片加载完都不触发 ⇒ 圆点停在旧高度；回调**跳过 streaming 的记录**（否则每 chunk 都测量，毁掉本模块"流式期间零布局读取"的设计），`measure()` 增加"容器高 0 就不测"的守卫（隐藏面板会把所有点钉到 top 0 并当成真相）；**071** 用户消息的折叠判据由"有换行 **或** 长度 > 160"改为**实测渲染行数**：先建出真正会渲染的 details（caret 与图标都在，宽度才对）、插入后用 `Range.getClientRects()` 数行盒，二分找第一行末尾、回退到词边界、`\n` 优先，量出来只有一行就**退回普通气泡**；切分点落在 `\n` 时那个换行符**被丢弃**（summary↔body 的块边界已经是一次换行，留着会多出空行）；量不出来（隐藏面板 / 无 createRange / 超 2000 字）回退旧判据并标 `data-fold="heuristic"`，等它拿到真实尺寸后**只重判一次**（两个方向都要能改：折→不折、不折→折）；**072** 思考块改走 markdown 往返（`buildThought` 原先写纯文本 ⇒ 字面反引号与字面 `- ` 列表符可见）：`ThoughtEntry.html` + store 的 thought 分支补 `html`、**合并分支清 `html`**（助手分支一直有）、流式期间只渲染纯文本（只从定稿的 patch 排 pending，避免把某个前缀的 html 冻住）、思考块图标/三角的重写规则不变、`.md` 与 `.bubble` **共用**一套 markdown 排版规则（.thought-body.md 回到 `white-space: normal`，代码块/表格内重置斜体）；顺带修 GFM 任务列表（`marked` 默认输出 `<input>`，客户端白名单没有 INPUT 就被静默 unwrap ⇒ 勾选态完全消失；改为覆盖 `checkbox` 钩子输出字形，白名单不放表单控件）；**073** 模型/模式/配置项切换在记录区留一条**分隔式提示**（"Switched to …"）：新增纯函数 `sessionChoices.ts`（取快照 + 算差异 + 一句人类可读的话），宿主 `syncChoices` **独占**快照缓存 ⇒ 我们自己的 setter 与 agent 的 `config_option_update`/`current_mode_update` 两条上报路径谁先到谁提示、后到者 diff 为空，天然去重；首次见到某会话只做基线（不打印"切到它本来就有的模型"）；**074** 工具卡显示 agent 自己报的工具名（`_meta.claudeCode.toolName`，夹具里 12/12 都有）并与 kind 标签并列（kind 是 ACP 的粗粒度词表，两张 "Run" 卡靠它才分得开）；**075** 会话改名提示（首次自动标题只做基线、不提示）+ `agent_thought_chunk` 里的非文本内容不再被丢弃（先关掉思考块再建 content 记录，否则旧块会永远停在 streaming）。`sessionChoices.ts` 的差异判定另有 7 条单测（含"同一次切换被上报两次只提示一次"）。**076 会话大纲钉住式右侧栏**：`#outlineBtn` 从 header 最左移到最右（`#nestToggle` 之后，`↺` 留左）；大纲内容从「只收 user」扩到「user + assistant」，每项加类型图标（`NS.icons` 新增 `icon` 导出）+ 原生 title 全文（hover 看更多）；新增钉住形态——下拉头部「固定到右侧」（`#outlineBar`，静态、不被 `render()` 清掉）把大纲钉成常驻右栏（`#outlineSidebar`，`#messages` 的 flex 兄弟、定宽可拖拽 180px~50%）；`mode`/`width` 走 `persistUi`（vscode.setState）持久化；侧栏跳转不关（常驻导航）、下拉维持「跳完收起」、点击外部只关下拉；`close()` 在侧栏模式下 no-op（会话切换不把刚恢复的侧栏关掉）；`transcriptView` 新增 `messageAnchorCount()`（user+assistant，O(1) 驱动 ☰ 显隐）。纯客户端，不动协议/命令/package.json。**077 大纲二次优化（用户反馈 4 项）**：①message 类型区分——图标按 kind 着色（`.outline-kind-user` 蓝、`.outline-kind-assistant` 灰，与 transcript 气泡同色系）；②时间右移 + 秒级——`.outline-time` 从图标与文本之间移到最右（正文 `flex:1` 占满左侧），`timeLabel` 输出 `HH:MM:SS`；③钉住持久化搬到宿主 `globalState`——新增非会话作用域 `setUiPref`（webview→ext，宿主存 `acpc.outlinePrefs.v1`）与 `uiPrefs`（ext→webview，随 boot 带回），`ChatPanelHost` 构造新增可选 `globalState` 形参（extension.ts 传 `context.globalState`），`outline.init` 不再从 `vscode.setState` 读、改由 boot 后 `applyPrefs` 恢复——跨窗口重载/编辑器面板重开都存活；④钉住按钮并进 `.outline-head`（"N messages" 同一行），动态计数移入 `.outline-head-info` 子容器、render 只清它，删掉独占一栏的 `.outline-bar`。 | keep-ours | active |
| src/test/chat-panel.test.ts + src/test/chat-client.test.ts + src/test/fixtures/（无标记：测试文件与测试数据） | （测试文件，无标记） | 20260925-054→055→056→057→058→063→068→069→070→071→072→073→074→075→076→077 | **聊天面板的第一层自动回归测试**（`CUSTOM-20260925-054` 新增）。在真 Extension Host 里构造真实的 `SessionManager` / `ConnectionManager` / `AgentManager` / `ChatPanelHost`，用桩 `ChatSurface` 拦下宿主**准备发给 webview 的每一条协议消息**，再用**真实抓包的 replay 夹具**驱动它。三个套件：`isBlankText polarity`（5 组用例，最后一组用夹具里**每一条真实正文分片**当判据——被误分类的正是那批字符串）、`real session/load replay`（replay 正文不得被静默丢弃 / 不得留下永远 streaming 的记录 / 快照与增量一致 / live 对照组）、以及一条打印 live vs replay 通知形状的**诊断**（下次再有人报"重开不一样"，先看这张表：**是 agent 没回放，还是我们丢了**）。夹具由 [`CUSTOMIZATIONS/scripts/capture-acp-replay.mjs`](./scripts/capture-acp-replay.mjs) 用真实 agent 抓取（`initialize → session/new → prompt（可多轮）→ session/close → session/load`，live 与 replay 两段流写进同一文件），约 450KB，**刻意不裁剪**——手工裁剪等于把我对"什么重要"的假设塞回夹具，而它的价值恰恰在于未经加工。**它不做 DOM 测试**：布局类问题（空白条 / 被压扁 / diff 被折叠 / 按钮化后外观错位）仍需人工验收，原因写在文件头（这一层能判定的三类问题全在协议流上可判，且不依赖 webview、不会因布局而脆）。**这一层已经当场抓到 055**（首跑失败暴露的是测试脚手架自己的问题：宿主走合帧队列，同步测试必须显式冲刷——改用公开的 `onFocusChanged`，它恰好就是"重新聚焦/重开"那条路径）。**056/057 追加**：`default working directory policy`（5 条，纯函数 `pickDefaultCwd` 的三级回退与两种拒绝）、`recent directories for the picker`（2 条，用内存假 Memento，不碰任何真实设置）、`history picker carries the session directory`（2 条，用**记录型子类**而非手写桩，断言用户选的 cwd 一路传到打开调用）。**058 追加**：`draft page creates the session on first send`（5 条）——选中的目录一路传到 `createSession` 且随后真的发了 prompt / 空草稿不建任何东西 / 建会话失败时回 `draftFailed` 且**不消费草稿** / 目录候选三字段齐全且默认目录非空 / connect **只** `ensureConnected` 不建会话。用的是 `RecordingSessionManager` 式的子类（覆写 `createSession`/`sendPrompt`/`ensureConnected` 记账），其余行为仍是真实现——比手写桩安全。**071-072 新增 6 条客户端逻辑用例**（`chat-client.test.ts`）：①**只折行、无换行的消息也折叠**（旧判据只认 `\n` 与长度阈值，这条正是用户两次报的那个 bug）；②**一行放得下就绝不折叠**，哪怕超过旧阈值 160 字（判据是实测行数，两个方向都测才说明它是**测量**而不是"更宽的阈值"）；③**量不出来时回退旧判据并标记 heuristic**，随后拿到真实尺寸**只重判一次**且能把已折叠的改回不折（单向实现会漏的那一半）；④⑤⑥ 思考块的 markdown 往返：流式期间**不**渲染、定稿后排进 batch、html 到达后 `.thought-body` 变 `md`、以及**后续 revise 不得把 markdown 换回原文**。桩 DOM 补了一个**确定性的 Range 模型**（每 N 字一行，可调），因此二分 + 空格回退 + `\n` 优先级都是按**纯逻辑**被测到的——**不假装测布局**（那是浏览器的事，用近似实现只会给假信心，见 pitfalls #25）。**076 追加 2 条大纲逻辑用例**（`chat-client.test.ts`，桩 DOM 补 `classList.toggle`）：锚点只收 user+assistant（thought 被排除）、每项 = 图标 + 时间 + 摘要 + `data-jump-id` + title、摘要截到 80 字带省略号。**077 追加**：大纲用例随改动更新——图标断言改成 `.outline-kind-user/-assistant`（着色）、时间断言两个冒号（HH:MM:SS）、桩 drawer 补 `.outline-head-info` 结构。 | keep-ours | active |
| src/test/nesting.test.ts | （测试文件，无标记） | 20260923-013 | 新增 11 个单元测试钉死嵌套推断的行为，重点是「宁可漏连不可错连」的护栏（等跨度不连、并列不连、子不可早于父、深度上限、显式证据优先于时序猜测）。测试文件按机制约定不要求 `[CUSTOM-BEGIN]` 标记 | keep-ours | active |
| src/ui/StatusBarManager.ts | 20260923-001 | 20260923-001 | 文件头标记：状态栏点击命令改为 `acpc.connectAgent` | merge-manual | active |
| src/config/AgentConfig.ts | 20260923-001/030 | 20260923-001→030 | ①文件头标记：`getConfiguration('acpc')`（读 `acpc.agents`）；②**030** `getAgentNames()` 把主用 agent（`Claude Code`）**稳定**提到第一位——顺序来自 `acpc.agents` 的键序，用户自建配置很容易把它排到后面；只做「提到最前」，不做全排序（否则打乱用户刻意安排的次序）。与 `panelContract.MODERN_AGENTS` 是两件事（那个管「哪些 agent 用新面板」），取值相同但理由不同，**不合并** | merge-manual | active |
| src/handlers/PermissionHandler.ts | 20260923-001/020 | 20260923-001→020 | ①文件头标记：`getConfiguration('acpc')`（读 `acpc.autoApprovePermissions`）；②**020 改走权限桥**：自动批准分支、`ignoreFocusOut`、两处遥测**原样保留**，末尾把 QuickPick 那段提成 `private quickPick(params, label)`，由 `PermissionBridge` 决定「面板内卡片」还是「退回弹框」；`label` 标明是哪个会话在请求（并发请求不再出现两个分不清归属的弹框）。回归 #14（非 Claude Code agent 行为不变）靠这条分支不被改动 | merge-manual | active |
| src/handlers/PermissionBridge.ts | 20260924-020 | 20260924-020 | **新增**（上游无此文件）。`session/request_permission` 的唯一出口：能进面板就发卡片，进不去才排队弹框。它存在的理由是这条链有三条泄漏路径：①**SDK 无超时**，呈现不出来就是 agent 永久等待；②**取消轮次时必须回答 `cancelled`**（ACP 契约，上游完全缺失）；③**并发请求**在上游会叠出多个不可区分的 QuickPick。不 import vscode UI 类型（只认 `PermissionPresenter` 接口），因此可单测驱动。`cancelSession` / `cancelAll` / `settle` 保证每个请求恰好被回答一次 | keep-ours | active |
| src/utils/Logger.ts | 20260923-001 | 20260923-001 | 文件头标记：输出通道名改为 `ACP Client (Custom)` / `ACP Traffic (Custom)`；`getConfiguration('acpc')`（读 `acpc.logTraffic`） | merge-manual | active |
| src/utils/TelemetryManager.ts | 20260923-002 | 20260923-002 | **整体重写为 no-op**：删除硬编码的上游 Application Insights 连接串 `InstrumentationKey=c4d676c8-…`，不再 import `@vscode/extension-telemetry`；保留 `initTelemetry` / `sendEvent` / `sendError` / `sendException` 同名 API（空实现 + `_` 前缀未用参数）以免调用点扩散 | keep-ours | active |
| src/test/extension.test.ts | （测试文件，无标记） | 20260923-001 | 断言更新：扩展 id `zouv.acp-client-custom`；命令前缀断言 `acpc.`；`acpc.connectAgent` / `acpc.newConversation` / `acpc.openChat` | keep-ours | active |
| src/core/SessionManager.ts | 20260923-010/020/057 | 20260923-010→020→057 | **多会话 / 多 agent 并行改造**（上游为「单活跃会话」模型）：①`agentSessions` 由 1:1 映射改为 `Map<agentName, Set<sessionId>>`；②新增 `agentProcesses`/`agentProcessNames`（agent 名 ↔ 进程 id 双向索引，作为「是否已连接」的真相源——仅探测无会话的 agent 现在也正确报告已连接）、`inFlightTurns`（每会话在途轮次守卫）、`connectionQueues`（每连接控制面串行化，仅包 `session/new`/`load`/`resume`/`close`；`prompt` 刻意保持并发，那正是本功能的意义）；③**删除** `connectToAgent` 强制断开前一个 agent、`newConversation` 的 disconnect+reconnect、`loadSession`/`resumeSession` **驱逐同 agent 旧会话但不杀进程**（进程泄漏不可回收的根因）三处逻辑；④新增 `createSession`/`closeSession`/`focusSession`（焦点变更的唯一收口点）；⑤生命周期监听器移到构造函数一次性注册——修上游「每次 connect 注册一对监听器且从不移除」的 EventEmitter 泄漏，以及 `ensureConnected` 路径完全不注册导致无错误/退出处理的盲区；⑥`sendPrompt` 第二参数放宽为 `string \| ContentBlock[]`（为附件 `resource_link` 铺路）并加在途守卫；⑦`summarizeCapabilities` 补 `close`/`fork`（`close` 是多会话干净关闭所需）；⑧事件契约**仅追加尾参、不改名**：`active-session-changed` 追加 `agentName`、`agent-closed` 追加 `agentName`、`clear-chat` 追加 `(agentName, sessionId)`，新增 `session-created`/`session-closed`；`activeSessionId` 语义由「唯一活跃」变为「聚焦」。**020**：新增 `setPermissionBridge`（晚绑定，理由同 `setHistoryStore`）；`cancelTurn` **第一句**先 `cancelSession` 再发 `session/cancel`——ACP 契约要求轮次取消时客户端必须把待决的 `session/request_permission` 回答成 `cancelled`，否则 agent 永久挂起（上游完全缺失这条）。**033**：新增 `openExistingSession(agentName, sessionId)`——「打开一个已存在的会话」的**唯一决策点**（优先 `session/load` 重放历史，退化为 `resume`），树命令与面板历史选择器共用；抽出的理由是两处各自判断迟早会漂移成"面板和树打开方式不一样"。**057 每会话工作目录**：`getWorkspaceCwd()` → **`resolveDefaultCwd()`**（公开，三级回退：`acpc.defaultWorkingDirectory` → 第一个工作区文件夹 → `process.cwd()`；相对路径或不存在的目录**被拒绝并记日志**，不交给 agent——坏 cwd 会让 `session/new` 在适配器深处失败）。`createSession(agent, {focus?, cwd?})` 的 cwd 可选，并作为**进程 spawn 的偏好**传给 `ensureConnected`（只影响首次 spawn）。`loadSession` / `resumeSession` / `openExistingSession` 接受 cwd，解析顺序是「调用方给的 → 本地历史缓存里那条会话自己的 cwd → 默认」——**这修掉了一个现网 bug**：历史选择器展示的是 agent 侧跨目录的 `session/list`（用户截图里 245 条），而点开时宿主从不传 cwd，于是告诉 agent 的是**当前工作区**，一条属于别的项目的会话会被当成住在这里重开。`pickDefaultCwd(...)` 抽成**纯函数并导出**，`resolveDefaultCwd()` 只是它的 vscode 外壳——抽出来的理由是**可测**：直接驱动 `getConfiguration().update()` 写设置意味着测试要去改用户/工作区的真实设置文件 | merge-manual | active |
| src/core/AgentManager.ts | 20260923-010 | 20260923-010 | 生命周期事件补充 agent 名（`agent-error` / `agent-closed` 事件体加 `name` 字段），使 SessionManager 能在单一订阅点把 `agentId` 反查回配置键。纯新增字段，现有消费者按需解构，无破坏性 | merge-manual | active |
| src/core/SessionHistoryStore.ts | 20260923-010/057 | 20260923-010→057 | **多会话支持：活跃会话豁免**。新增 `liveSessionIds` 与 `setLiveSessions()`（由 SessionManager 在会话索引变化时推送），`enforceCap` 与 `reconcileFromAgent` **永不**驱逐/剔除活跃 sessionId——多会话会大幅加速会话创建，否则活跃会话会被容量淘汰或 `session/list` 对账删掉，从树上消失而进程仍在跑。`STATE_KEY` 仍为 `acp.sessionHistory.v1`（`workspaceState` 的 Memento key，扩展内作用域，**故意保留**，改名会丢既有历史，见 pitfalls #4）。**057 新增 `recentDirectories(agentName)`**：为「新建会话时选目录」的「最近用过」组提供来源，复用现成的 `list(agentName)`（不传 cwd = 全部）——它本就按 `lastActiveAt` 降序，所以**首次出现的目录就是它最近一次被用的时刻**，不需要额外时间戳。**作用域注意**：该 store 在 `workspaceState` 里，只记得**本工作区曾用过**的目录，新工作区开局是空的（下一步会把它与 agent 侧的跨目录清单合并） | merge-manual | active |
| src/core/ConnectionManager.ts | 20260923-012/020 | 20260923-012→020 | `ConnectionInfo` 新增 `terminals: TerminalHandler` 字段并在 `connect()` 里填入。原因：`TerminalHandler` 此前只在 `connect()` 里作为局部变量存在（仅被 `AcpClientImpl` 的私有字段引用），聊天面板拿不到任何句柄，工具卡片上的终端 chip 是无数据的死控件。**020**：构造函数接收 `PermissionBridge` 并传给 `new PermissionHandler(bridge)`——每个连接都会新建一个 handler，而它此前拿不到任何 UI 服务。`clientInfo.name` 仍是 `vscode-acp-client`——那是 ACP `initialize` 上报给 agent 的客户端名，协议元数据，**故意保留**（见 pitfalls #4） | merge-manual | active |
| src/handlers/TerminalHandler.ts | 20260923-012 | 20260923-012 | 新增只读访问器 `readOutput(terminalId)`：返回 `{output, truncated, exited, exitCode, exitSignal}`，未知 id 返回 `null` 而不是抛异常。ACP 的 `Terminal` 工具内容只是引用（`{terminalId}`），终端归客户端所有也由客户端渲染；上游唯一的读取路径 `terminalOutput()` 对未知 id **抛异常**，不适合 UI 调用 | merge-manual | active |
| CUSTOMIZATIONS/（README.md、registry.md、architecture.md、docs/pitfalls.md、docs/changelog.md、docs/dev-workflow.md、docs/arch/chat-panel.md、docs/arch/chat-panel-sessions.md、docs/arch/chat-panel-records.md、release-notes/、patches/、scripts/） | （纯自定义目录） | 20260923-000→015→016→042→054→060 | 自定义开发机制：规则唯一源 + 改动账本（**总览在 registry.md、日志在 docs/changelog.md，016 拆分**） + 代码地图 + 坑点库 + 本地开发与验收流程。**`src/` 是死的**：不在 tsconfig/webpack 构建图内，放那里的 `.ts` 编译不了，新代码请放仓库根 `src/`（016 修正了原先教错的做法）。移植自 zouv/custom-chatbox 并按 VS Code 扩展语境适配（npm / webpack / .vsix / 上游无 tag）。**042 拆分 `architecture.md`**：该文件达 539 行，已超它自己在文件头写下的「体量铁律（超过 ~400 行即拆分，保留 §0 / §0.5 / §1，模块细节拆到 `docs/arch/<module>.md`）」。把原 §5（新 Chat 面板子系统，12 个小节，占 58% 体量）整体移入新增的 `docs/arch/chat-panel.md`，主文件回落到 244 行。**关键决策：§5.x 的编号原样保留**——代码注释与其它文档里有几十处「§5.4」「§5.10」引用，重编号会把它们全变成指向空处的雷，而拆分的目的是省上下文不是整理编号；因此拆分只做「换文件名」，并同步修掉所有引用（`architecture.md` 内部 6 处、`AGENTS.md`、`ChatEditorPanel.ts`、`ChatRouterProvider.ts`、`contentBlocks.ts`）。拆分的边界写在新文件头上：主文件保留「读到该改哪个文件」所需的全部（§0/§0.5/§1/§2–§4），按需加载的是「改 `src/ui/chat/**` 时才知道要读的东西」；下次再超 400 行时 §2–§4 是下一个拆分对象。`.vscodeignore` 无需改动（`CUSTOMIZATIONS/**` 已整体排除）。**060 二次拆分 + 同题合并**：`chat-panel.md` 到 488 行（超 §0 体量铁律），把 §5.13（replay 怎么测）/§5.14（每会话工作目录）/ §5.15（草稿页与目录选择）移入新增的 `docs/arch/chat-panel-sessions.md`（三节同属**会话生命周期**这一主题），主文件回落 385 行。**§5.x 编号原样保留**，因此本次拆分**不需要改任何外部引用**（§5.4/5.5/5.7/5.8/5.10/5.12 全留在主文件里）。同时按用户要求做**同题合并**：changelog 59 → 43 条（1161 → 1021 行），合并 14 项面板审计 → 2 条、白条问题 5 条 → 1 条、每会话 cwd 两步 → 1 条、053 并入 055；**所有 change-id 保留在合并后的标题/正文里**（范围写法 `038..041` 等），已用脚本核对 000–060 无缺失。**这是 changelog 唯一一次改写历史**（该文件契约是 append-only）——已在文件头写明合并范围与"之后仍 append-only"，代价也记了：合并后的措辞是后来重写的，不再是当时的原文。顺带修掉一个我自己的错误：056/057/058 曾被 `cat >>` 追加到文件**末尾**，违反"最新在最上"，已按(日期 desc, 序号 desc) 重排。pitfalls §11 的四条复发记录合并成一段「复发史 + 四次升级」，保留每一条不同的教训。 | keep-ours | active |
| CUSTOMIZATIONS/scripts/（init-repo.ps1、sync-vendor.ps1、list-custom.ps1、check-registry.mjs、check-webview-client.mjs、**dev-host.mjs**、**capture-acp-replay.mjs**、**probe-session-cwd.mjs**、release-vsix.sh、normalize-eol.mjs） | （纯自定义目录，逐文件标记非必需） | 20260923-000→005→008→011→012→014→054→056→058 | 仓库基线初始化、vendor 同步（`-Ref` 代替 `-Version`）、改动清单查询、一致性自检（六节：标记↔账本 / 反向校验 / 命名空间卫生 / 换行符 / **webview 客户端脚本可解析性** / **源码控制字符卫生**，**Node 实现**）、webview 内联客户端脚本语法自检、**本地启动测试环境**（`npm run dev:host`）、.vsix 打包封装、换行符归一化工具。**011 两处修复**：①`normalize-eol.mjs` 原先用 `git ls-files`（只列已跟踪文件），新建未提交的文件会静默绕过 CRLF 检查——改为 `--cached --others --exclude-standard`（pitfalls #10）；②`check-registry.mjs` 新增 §6 扫描 NUL 等控制字符（pitfalls #12）。**012 增强**：`check-webview-client.mjs` 新增**模板体内反引号检测**——此前这类错误只能靠 tsc 报出难以定位的 TS1005（pitfalls #11 的第二次复发）。**014 新增**：`dev-host.mjs`。**017**：`check-webview-client.mjs` 的扫描范围从 `html/client/` 扩到整个 `html/` 树——原先漏扫 `styles.ts`/`body.ts`/`shell.ts`，导致「tsc 报错而检查器说 OK」。**054 新增 `capture-acp-replay.mjs`**：用真实 agent 抓一份 ACP `session/load` replay 当回归夹具（见 `src/test/chat-panel.test.ts` 那一行）。它实现了一个**最小但忠实**的 ACP client——能力集与扩展一致（`fs` 读写 + `terminal`，因为 agent 行为可能取决于宣称的能力），终端能力真的接 `child_process`（不实现的话 Claude Code 的 Bash 工具会让整轮失败），权限一律自动批准（否则卡在等人点按钮）；**在一次性临时目录里跑、绝不在仓库内跑**，并把 live 与 replay 两段流写进同一个文件，好让测试能做"实时 vs 重开"的对比。用法与注意事项写在脚本头部。**注意它会真的拉起 agent、真的花额度、并在该 agent 的会话历史里留下一个新会话**——夹具是一次性产物，只在协议形状变化时才需要重抓。**056 新增 `probe-session-cwd.mjs`**：回答「agent 适配器是否遵守**会话级** cwd」的探针，是「每会话一个目录」可行性的**闸门**。做法非对称、一次就够：两个临时目录 A（进程 cwd）/ B（会话 cwd），各放一份**同名不同内容** `which-dir.txt` 与一个**只在一边存在**的文件；用 cwd=A 拉起 agent 后 `session/new({cwd:B})`，让 agent 跑 `pwd`/`ls -1` 并读**相对路径**。判定机械——标记字符串只会出现在输出里。**结论 `honours-session-cwd`**：`pwd` 报会话目录、`ls` 只看到会话那边、读相对路径得到会话那边的标记，进程目录的标记一个都没出现 ⇒ **改一条链就够，不需要按 cwd 分进程**。附带发现：`terminal/create` 与 `fs/read_text_file` 各被调用 **0 次**——Claude Code 的 `Terminal`/`Read File` 是**适配器自己执行**的，不走客户端能力，因此每会话目录不依赖我们的 `TerminalHandler`，而 `FileSystemHandler` 的「优先返回未保存编辑器缓冲区」对它**从不生效**。**058 给 `check-webview-client.mjs` 加了 `--fix`**：把**模板体内**（只在模板体内，文件头注释不受影响）的反引号就地换成单引号——这类错误在客户端代码上复发过 7 次，而修法永远是同一个机械动作，按 pitfall #11 自己的教训「能自动化的纪律就不要留给记忆」。**做过负向测试**（注入 6 处 → 报错 → `--fix` → 只改那两行），且按 `
` 切分拼接所以 **CRLF 不会被转成 LF**（pitfall #7）。`--fix` 是手动用的：`check-registry.mjs` §5 与 CI 调它时**不带参数**，闸门仍然只报错不改代码 | keep-ours | active |
| .gitattributes | 20260923-005 | 20260923-005 | **新增**。写 `* -text` 关闭 git 的换行转换。上游 blob 以 CRLF 入库且本机 `core.autocrlf=true`，不固定的话编辑工具一转 LF 就产生整文件伪差异、并毁掉三方合并能力（见 pitfalls #7） | keep-ours | active |
| .agents/skills/（acp-record-change、acp-merge-upstream、acp-release） | （纯自定义目录） | 20260923-000→016 | 3 个 AI Agent 工作流 skill：改动登记、上游合并（适配无 tag 的 vendor/main）、打包发布（.vsix + GitHub Release） | keep-ours | active |
| AGENTS.md | （纯自定义文件） | 20260923-000→015 | AI Agent 会话级硬约束摘要：必读文件、工作流、技术栈（npm/webpack）、构建命令、分支规则、自定义代码规范、skills 触发表、文档更新职责；含会话礼仪约定（回复开头称"啊唯"）。**015 起刻意保持精简**——`AGENTS.md` 每个会话都会被加载，属于"每次都要付的上下文税"，详细流程一律下沉到 `CUSTOMIZATIONS/docs/` 并在本文件留一行指针。**016 进一步减重**（147→121 行）：删除与 `architecture.md` §1 重复的「项目结构速查」、技术栈只留会破坏构建的两条、补上「check-registry 必须 exit 0」与「EOL 归一」两条真正缺失的硬约束 | keep-ours | active |
| .github/workflows/publish.yml | 20260923-004/006 | 20260923-004→006 | ①移除 `Publish to Visual Studio Marketplace` 与 `Publish to Open VSX Registry` 两步（上游依赖 `secrets.VSCE_PAT`/`secrets.OVSX_PAT`，本仓库无这些 secret）；②去掉 `release: created` 自动触发，改为**仅 `workflow_dispatch`**——发布主路径是本地 `gh release create`（acp-release skill），保留自动触发会在手动发布后再挂一个冗余的 `extension.vsix`；③workflow 名 `Publish` → `Package (Manual)`，job `publish` → `package`，产出改为 workflow artifact（`actions/upload-artifact`），**完全不接触 GitHub Release**；④`npx vsce` → `npx @vscode/vsce`。**文件名刻意保留 publish.yml**（上游也有此文件，改名会产生 delete/modify 冲突） | merge-manual | active |
| .github/workflows/ci.yml | 20260923-007/008/009 | 20260923-007→008→009 | ①**修 CI 完全失效的问题**：上游触发分支是 `[main]`，但本仓库（fork）没有 `main` 分支（默认与开发分支是 `custom/main`），导致 CI 从未触发过——改为 `[custom/main]`；②新增「Check custom-development invariants」步骤，用 `node CUSTOMIZATIONS/scripts/check-registry.mjs` 跑机制自检（代码标记↔账本 / 命名空间卫生 / 换行符卫生），把约束从口头约定变成 CI 强制；③`npx vsce package` → `npx @vscode/vsce package`；④**macOS 上跳过 `npm test`**——`@vscode/test-electron` 在 macos-latest 上解压静默失败（299MB 报 12 秒下完，随后 Electron ENOENT），属上游既有问题（上游 CI 历史上从未成功过）；macOS 仍保留机制自检与打包，**跨平台自检覆盖必须留着**（见 pitfalls #9） | merge-manual | active |

---

## 变更日志

> **本节已移至 [`docs/changelog.md`](./docs/changelog.md)。**
> 变更日志是 append-only 的历史流水，占原单体文件约 70% 体积——按需查阅即可
> （只在需要追查某次改动的来龙去脉、或核对「这轮到底改了什么」时才读）。
>
> **本标题刻意保留**：`check-registry.mjs` 与 `list-custom.ps1` 都用它作为「改动总览表格到此为止」
> 的终止哨兵，保留它可以让两个解析器零代码改动继续工作。**不要删掉这一行。**
